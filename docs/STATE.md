# KEYRA — Onde Paramos (snapshot executivo)

> **Data deste snapshot:** 2026-05-31 (EPIC-COMPROVANTES `.1` — fundação de banco APLICADA EM PRODUÇÃO) — 🧾 **Jornada inteira `.1 → .4b` autorizada (recorte máximo + apply em prod). `comprovantes.1` aplicada no `keyra-br` e validada localmente; falta só a confirmação pós-apply em prod.** Entregue: spike RLS de Storage 🟢 VERDE (`scripts/spike-storage-rls.sh` + `docs/spikes/comprovantes-1-storage-rls-spike.md`), 3 migrations (`20260531000100_receipts` + `…000200_…source_type_document` + `…000300_…storage_bucket`) **aplicadas via `supabase db push`** e registradas no remoto, tabela `public.receipts` (RLS catch-all via `current_org_id()` + FORCE + trigger dedicado `audit_receipts` minimalista LGPD), `transactions.source_type += 'document'`, bucket privado `receipts` + 4 policies em `storage.objects`, suíte RLS estendida (Block I + stubs de `storage` no `run-rls-tests.sh` + CI), contrato `purgeOrgStorage` (SPEC §12.1, TD-CMP-003 rastreado), runbook de rollback. **Gates `@data-engineer` (Dara) + `@compliance-br` (Têmis): APPROVE.** **MCP `supabase` oficial adicionado** ao `.mcp.json` (project scope, keyra-br) — aguardando aprovação/OAuth do Luiz. **Retomar por:** `docs/sessions/2026-05-31-retomada-comprovantes-jornada.md` → rodar `scripts/verify-comprovantes-1-prod.sql` (confirmação pós-apply) + `pnpm typegen` → fecha `.1` → segue `.2/.0/.3/.4a/.4b`. **Pendências:** remote git `luizxhgomes/keyra` quebrado (push/PR/deploy — `@devops`); `AI_GATEWAY_API_KEY` para `.3`. **Pode encerrar para autenticar o MCP e retomar.**

> **Data anterior:** 2026-05-19 (governança de design system) — 🎨 **Regra-mãe de design system criada, aplicada, auditada em CI e deployada.** PR #35 mergeado em `main` + redeploy `653f8fc`. **Entregue:** `.claude/rules/design-system.md` (8 princípios inegociáveis — tokens são lei, paleta quente, **Lei da Proporção Espacial** — com bloco editável demarcado para ajuste humano), `scripts/check-design-system.sh` + job CI `design-system-audit` (barra PR fora do padrão), registro em `.claude/CLAUDE.md`. **Correções aplicadas:** `StatusBadge.tsx` (4 badges migrados de `stone-*`/`rose-*` para tokens KEYRA quentes) e `apps/web/public/landing.html` (8 `<br>` manuais removidos — eliminam palavras órfãs nos títulos; `.cost-card`/`.app-msg` com conteúdo centralizado; `.plan-name` com piso de 2 linhas). **Incidente de cache:** a landing ficou presa no edge cache da Vercel (`x-vercel-cache: HIT`, age ~12h) servindo a versão antiga — redeploy `653f8fc` invalidou; produção renderizada via headless Chrome e verificada OK. **Natureza:** tarefa direta de `@aiox-master` (não-story) — NÃO altera a fila do EPIC-COMPROVANTES. **Pendente:** smoke visual humano da landing em aba anônima (mobile + desktop). **Próximo (inalterado):** `@dev *develop-story comprovantes.1`. **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-18 (planejamento EPIC-COMPROVANTES) — 📋 **EPIC-COMPROVANTES (Phase 7.0 — Comprovantes Inteligentes) planejado, revisado, validado e mergeado em `main`.** PR #34 (`1d7bac8`, squash, 14 arquivos, +3.144 linhas) entregou só planejamento — zero código de aplicação. **A feature:** a usuária anexa um comprovante em qualquer formato (foto, print, PDF, DOCX, ODT, TXT, MD, HTML, EPUB), uma LLM (`openai/gpt-4o-mini` via Vercel AI Gateway) lê e extrai os dados financeiros, ela revisa e o sistema registra como `transactions` — receita ou despesa. **Artefatos:** ADR-023 (`ARCHITECTURE.md` §11.3 — provider lock + arquitetura), `EPIC-COMPROVANTES-SPEC.md` (spec rev. 2), `EPIC-COMPROVANTES.md` (epic, 7 stories ~48 pts), `privacy-v1.1.0-DRAFT.md` (Política com uso de IA), `COMPLIANCE-AUDIT-EPIC-COMPROVANTES.md` (auditoria LGPD — 11 achados, CONCERNS endereçável), 2 pareceres em `docs/architecture/reviews/`, 7 stories `comprovantes.0`–`.5`. **Processo:** spec revisada por `@architect` + `@document-processor` (NEEDS_REVISION — 3 bloqueadores factuais corrigidos: CHECK `transactions.source_type` sem `'document'`, PDF não-nativo no `gpt-4o-mini`, RLS de Storage sem precedente; story `.4` dividida em `.4a`/`.4b`) → 7 stories draftadas (`@sm`) → validadas `@po` GO 10/10 cada (`Ready`) → auditoria de consolidação fechou o achado órfão CMP-M2 → `@devops` commitou e mergeou o PR #34. **2 spikes obrigatórios na implementação:** `.1` (RLS de Storage) e `.2` (rasterização de PDF em serverless). **Próximo:** `@dev *develop-story` a partir de `comprovantes.1`. **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-09 (refinamento sidebar + CI hardening) — ✨ **Badge flutuante editorial substitui botão "Recolher" do sidebar; CI agora tem secrets Supabase configurados.** Commit `00bbe35` (`feat(sidebar): badge flutuante editorial + animação warm-shadow`) entregue em `main` direto sem PR (mudança visual atômica de baixo risco). **Refinamento UX:** removido o botão "← Recolher" do rodapé do sidebar e adicionado um badge quadrado ivory ancorado no centro vertical da divisória direita (flutua sobre a borda entre sidebar e conteúdo); ícone alterna `PanelLeftClose` ↔ `PanelLeftOpen` para affordance imediata da ação; warm-shadow + scale-105 no hover; ring terracota em focus-visible; transição editorial em `transition-[width,box-shadow] duration-base ease-in-out-editorial` (320ms cubic-bezier 0.65,0,0.35,1) sustentando elevação durante o gesto. **CI hardening (paralelo):** secrets `SUPABASE_ACCESS_TOKEN` e `SUPABASE_PROJECT_REF` configurados no GitHub Actions via `gh secret set` — destrava o job `Auth recovery template drift check` que vinha falhando desde a configuração do workflow (não bloqueava deploy Vercel mas mantinha o badge de CI vermelho). **Validação técnica:** typecheck verde, ESLint zero warnings, build verde (42 rotas), RSC Boundary Audit PASS, Cross-tenant PASS, Vercel deploy SUCCESS (`https://keyra-edmpugpwk-luiz-henrique-sealdigital.vercel.app`), produção em `usekeyra.com` HTTP 200. **Pendente NÃO automatizável:** smoke real do gesto de toggle no desktop (≥lg) confirmando feel editorial — só humano valida (regra 4 RSC). **Próximo:** validação humana do badge ou seguir para opções A/B/C abaixo. **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-08 (sweep editorial completo + financeiro 3 fases) — 🎉🎉🎉🎉🎉🎉🎉🎉🎉🎉 **9 PRs MERGEADOS EM MAIN + REDEPLOY VERCEL FRESH.** Aplicação completa do padrão Editorial Beauty Luxury KEYRA em todas as listas e dashboards do MVP, fechando o ciclo de refinamento visual iniciado pelo Epic BRAND-INTEGRATION. **9 PRs entregues nesta sessão:** #22 Dashboard editorial (`b270a4b`) com 7 blocos (Hero saudação editorial Fraunces + 3 KPIs hero com pill buttons + Schedule heatmap mensal + DRE proporcional segmentado + Top Serviços + Major Expenses categorias filtráveis + Upgrade CTA cocoa-900); #23 Agenda Phase 1 (`a136f38`) layout 2-col com sidebar mini-cal + counts + próximo agendamento, status tabs em segmented control, search textual, ReceitaCard fora do flow sticky com cards animados stagger 80ms; #25 Pacientes perfil completo (`7f223ac`) layout 2-col com PatientHeaderCard avatar gradient + PatientOverviewCard + PatientPackageCard placeholder pacote/assinatura + 4 KPIs animados (Total gasto/Pendente/Atendimentos/Frequência) + tabelas atendimentos e pagamentos + seedDemoPatient cria "Maria Silva (Demo)" com 5 atendimentos + 3 comandas pagas + 3 transações; #27 Catálogo redesign (`436b57a`) com 4 KPIs MoM (Total/Ativos/Categorias/Sem categoria) + toolbar segmented + tabela unificada com pills coloridos por categoria; #28 Comandas (`9e73f70`) com 4 KPIs MoM (Total mês/Em aberto/Pagas mês/Faturamento) + status tabs em segmented control + lista refinada com avatar Receipt gradient; #29 DRE + Team (`3f3cf8a`) DRE com 3 SummaryCards no topo (Receita líquida terracota + Lucro success/destructive + Custos cocoa) e tabela com hierarquia subtotal/final em Fraunces 24px com cor por sinal; Team com 3 mini-stats inline + avatar gradient + RolePill paleta KEYRA; #30 Insumos (`ac32d5c`) com 3 mini-stats + avatar Package gradient mocha→bronze + pills "Recompra" amber e "Inativo" mocha; #31 Hub /financeiro Fase 1 (`ded90a0`) substituindo redirect por landing com 4 KPIs SVG sparkline inline (Receita/Despesas/Lucro/Saldo ano) + variação MoM colorida + stacked bars Jan-Dez (Receita terracota + Despesas cocoa) com motion stagger + comandas em aberto com priority flag (cliente recorrente = 3+ paid em 12 meses) + top 5 categorias com mini progress bars; #32 Financeiro Fases 2+3 (`ea453ff`) ExpenseTrackerAnimated com SVG path animado área cocoa/terracota + linha de média tracejada + highlight de hoje em círculo terracota, DailyCostingChart com barras da semana e hoje destacado em terracota com tooltip flutuante, VarianceTable em /metas com Realizado vs Meta mês a mês e status pills (▲ Atingiu success-leaf / ◐ Parcial amber / ▼ Abaixo rust-800 / Sem meta mocha). **Server Actions novas (8):** getCommandsStats, getCatalogStats, getPatientProfile/Encounters/Payments, seedDemoPatient, getTopServicesByRevenue, getExpensesByCategoryThisMonth, getMonthScheduleHeatmap, getMonthCounts (agenda mini-cal), getFinanceOverview, getOutstandingCommands, getTopExpensesByCategory, getExpenseAnalytics, getVarianceMonthly. **Princípio UX atualizado:** regra "zero gráficos" rebaixada de inegociável para preferência hierárquica (feedback idealizadora 2026-05-07) — números absolutos continuam protagonistas, mas tabelas e gráficos proporcionais simples (barras horizontais segmentadas, mini-bars de comparação, sparklines) permitidos como reforço quando ajudam a ler o todo. **Adaptações vs referências (KlarityOS + Kodo + Studio 813 + Prodless) com justificativa explícita:** WHT/VAT separados descartados (tributação asiática, KEYRA usa Simples Nacional); plano de contas Account No / Parent:Child descartado (ERP corporativo, KEYRA é gestão de clínica); A/P Aging descartado (sem fornecedores B2B com prazo); workflow "For Preparer" descartado (KEYRA é admin único); Client portal vs Super admin tools descartado (não é multi-tenant ERP); paleta roxa Kodo → cocoa/terracota canônica KEYRA preservada. **Redeploy fresh disparado** via commit empty `1ce33cd` (chore/ops trigger Vercel) + cada merge dos 9 PRs disparou auto-deploy. **Validação produção:** 8 rotas autenticadas respondendo HTTP 307 → /login (auth gate ativo) + /comecar HTTP 200 (sales page pública). Quality gates em todos os 9 PRs: typecheck verde, ESLint zero warnings, RSC Boundary Audit PASS. **Sistema 100% atualizado.** Pode encerrar terminal com segurança.

> **Data anterior:** 2026-05-07 (encerramento+sweep) — 🎉🎉🎉🎉🎉🎉🎉🎉🎉 **EPIC-BRAND-INTEGRATION 100% + sweep brand.9 (auditoria fiscal) MERGEADO em main.** PR #20 (`e459429`) squash mergeado; CI verde. **Story brand.9 (sweep complete)** fechou gaps identificados em auditoria fiscal honesta pós-Epic: 30 arquivos modificados (+90/-46), 10 secondary headings (h2/h3) migrados para Fraunces editorial cobrindo DRE/Despesas/Custos-fixos/Categorias/Metas/Estoque/Movimentações, sweep total de warm shadows em 10 arquivos (homepage CTA, despesa-form, redefinir-senha/sucesso, 4 cards auth, UserMenu, BottomNav, select.tsx — `shadow-sm/md/lg/xl` → `shadow-warm-*` via regex robusta), 3 cores Tailwind defaults remanescentes eliminadas (emerald-100/600 em redefinir-senha + SignUpCard, red-600 em PasswordStrengthMeter), stagger-list em CSS puro RSC-safe aplicado em 5 listagens (pacientes, comandas, despesas, transações, custos-fixos) via classe `.stagger-list` em globals.css com keyframe `keyra-fade-up` + delays nth-child 80ms canônicos + `prefers-reduced-motion` respeitado, voice migration expandida com +6 toasts cúmplices (paciente arquivado/reativado, despesa arquivada/reativada, profissional cadastrado, comanda finalizada, convite enviado — total 17 toasts cobertura ~20% dos 89). **Auditoria pós-sweep ZERADA em todas as métricas auditáveis:** 0 cores Tailwind defaults frias, 0 shadows não-warm, 0 h2/h3 sem font-serif, 6 listagens com stagger, 17 toasts cúmplices. Validação técnica: typecheck verde, lint zero warnings, build verde (38 rotas), RSC PASS, Cross-tenant PASS, Vercel SUCCESS. **Pendente NÃO automatizável (continua):** smoke real em iPhone 375px cobrindo 27 telas autenticadas — só pode ser feito por humano (Luiz/idealizadora). Sem essa validação, regra `docs/dev/rsc-boundary-rules.md` segue violada (mesmo bug das Sprints 5/6/7). **Próximo:** abrir Vercel preview em iPhone real para validar; ou criar story de refinamento baseado em feedback humano específico. **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-07 (encerramento) — 🎉🎉🎉🎉🎉🎉🎉🎉🎉 **EPIC-BRAND-INTEGRATION 100% CONCLUÍDO · 8/8 STORIES DONE EM MAIN.** PR #19 (`f48325b`) squash mergeado; CI verde, Vercel SUCCESS. **Story brand.8 (Remotion setup) entregou:** estrutura `apps/web/remotion/` com lib/tokens.ts (palette KEYRA tipada em TS + helper `msToFrames`), compositions/BrandIntro.tsx (vinheta 4s canônica do brandbook em 16:9 e 9:16, com wordmark letra a letra stagger 6f, gold sublinha animada, tagline italic Fraunces, fade-quiet out), Root.tsx registrando 2 compositions (`brand-intro-16-9` para apresentações e `brand-intro-9-16` para Reels/Stories), index.ts entry point, README.md com instruções de setup. **Decisão arquitetural:** deps Remotion (`remotion`, `@remotion/cli`, `@remotion/google-fonts`) são opt-in via `pnpm add` no README — não obrigatórias — para preservar velocidade do CI (Chromium headless ~300MB) e bundle do Next intocado. Configuração: `tsconfig.json` exclui `remotion/`, `eslint.config.mjs` ignora `remotion/**`. **EPIC BRAND-INTEGRATION FECHADO 100%:** Foundation (PR #12) → Components/keyra (PR #13) → Components/ui shadcn (PR #14) → Voice migration (PR #15) → Motion system (PR #16) → Auth flows (PR #17) → Sweep 27 telas (PR #18) → Remotion setup (PR #19). KEYRA agora tem identidade visual coerente da Sidebar até a vinheta de proposta comercial. Brandbook canônico em `docs/brand/` continua sendo a referência viva. Validação técnica final: typecheck verde, lint zero warnings, build verde (38 rotas), RSC Boundary Audit PASS, Cross-tenant PASS, Vercel SUCCESS. **Próximo:** Epic concluído. Stories futuras de refinamento visual por tela (oportunidades específicas) ficam para iterações pontuais. Pode encerrar terminal com segurança.

> **Data anterior:** 2026-05-07 (continuação 7) — 🎉🎉🎉🎉🎉🎉🎉🎉 **EPIC-BRAND-INTEGRATION 7/8 stories Done (87.5%) · brand.1 + brand.2 + brand.3 + brand.4 + brand.5 + brand.6 + brand.7 MERGEADAS em main.** PRs #17 (`bc0c521`) e #18 (`b043d60`) squash mergeados; CI verde, Vercel deploys SUCCESS. **Story brand.6 (Auth flows) entregou:** wordmark "KEYRA." (Fraunces text-3xl + ponto gold-500) aplicado em 4 cards de auth (SignInCard, SignUpCard, RequestResetCard, NewPasswordCard); onboarding com Fraunces no título + copy reforçando conceito Cadeia Viva ("A partir do primeiro atendimento, a operação vira financeiro sozinha"); migração de cores Tailwind defaults (emerald → success-leaf, red → rust-800, amber → palette KEYRA) em SignInCard/PasswordStrengthMeter; warm shadows aplicadas (shadow-warm-xl em cards principais). **Story brand.7 (Sweep das 27 telas) entregou via componentes compartilhados (alto impacto, baixo risco):** 16 page titles migrados para Fraunces editorial (`font-serif text-display` / `font-serif text-display-hero`) cobrindo dashboard/agenda/pacientes/servicos/comandas/financeiro/team/estoque/configuracoes/mais — layouts compartilhados afetam todas as sub-rotas; Sidebar com wordmark KEYRA. signature consistente com brandbook (bolha "K." em Fraunces + ponto champagne sobre primary, wordmark expandido em Fraunces text-lg + ponto gold-500, motion tokens duration-fast + ease-out-soft); 3 cores Tailwind defaults remanescentes eliminadas (emerald-500/700, amber-700). **Decisão arquitetural na brand.7:** em vez de redesenhar 27 telas individualmente (alto risco RSC + retrabalho), aplicado princípio brandbook canônico — mudanças em componentes compartilhados afetam todas as telas; refinamentos pontuais por tela ficam para iterações futuras. Validação técnica: typecheck verde, lint zero warnings, build verde (38 rotas), RSC Boundary Audit PASS, Cross-tenant PASS, Vercel SUCCESS. **1 story restante:** brand.8 (Setup Remotion + Brand Intro template, S ~3pts, paralelizável). **Próximo:** abrir brand.8 para fechar o Epic 100% ou encerrar sessão por aqui (87.5% já é entrega substancial). **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-07 (continuação 5) — 🎉🎉🎉🎉🎉🎉 **EPIC-BRAND-INTEGRATION 5/8 stories Done (62.5%) · brand.1 + brand.2 + brand.3 + brand.4 + brand.5 MERGEADAS em main.** PR #16 (`f71fa06`) squash mergeado; CI verde, Vercel deploy SUCCESS. **Story brand.5 (Motion system) entregou:** lib/motion/tokens.ts estendido com 5 variants canônicos brandbook (kpiRevealItem, kpiRevealContainer, staggerList, staggerListTight, pageEnter) + 2 easings novos (outSoft `cubic-bezier(0.22,1,0.36,1)` DEFAULT do brandbook + inQuiet); KPICard com KPI reveal narrativo em 3 atos (label → value → comparativo, stagger 80ms entre cada, AnimatePresence interno preservado para mudanças dinâmicas via P5 pattern); novo helper `<StaggerList>` em lib/motion/stagger-list.tsx para listagens com cadence default (80ms) ou tight (40ms para ≥10 items, suporta `as: 'div' | 'ul' | 'ol' | 'section'`). **Decisão arquitetural honesta:** View Transitions API (Next 16) ficou de fora — `unstable_ViewTransition` ainda tem inconsistências RSC (mesmo tipo de bug que quebrou Sprints 5/6/7); decisão conservadora ficou com Framer Motion via LazyMotion (RSC-safe + bundle preservado ~21KB). Validação: typecheck verde, lint zero warnings, build verde (38 rotas), RSC Boundary Audit PASS, Cross-tenant PASS, Vercel SUCCESS. **3 stories restantes:** brand.6 Auth flows (M, todas dependências satisfeitas), brand.7 27 telas (XL, todas deps satisfeitas), brand.8 Remotion setup (S, paralelizável). **Próximo recomendado:** brand.6 (auth flows com novo visual hero) ou brand.7 (sweep das 27 telas, vou quebrar em sub-stories durante draft). **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-07 (continuação 4) — 🎉🎉🎉🎉🎉 **EPIC-BRAND-INTEGRATION 4/8 stories Done (50%) · brand.1 + brand.2 + brand.3 + brand.4 MERGEADAS em main.** PRs #12 (`7c0fafd`), #13 (`d16f77b`), #14 (`ba3d396`), #15 (`cc6e636`) squash mergeados; CI verde em todas; Vercel deploys SUCCESS. **Story brand.3 (Components/ui shadcn) entregou:** 10 componentes shadcn migrados — button com motion tokens + warm shadow + variant premium (gold-500 + glow), card com warm shadow + CardTitle Fraunces, input/textarea com hover bronze (calor crescente), badge com 3 novos variants (success/warning/premium), checkbox com motion, alert-dialog/sheet com backdrop cocoa-900/60 (warm overlay), dropdown-menu/select com shadow-warm. **Story brand.4 (Voice migration) entregou:** Toaster (sonner) reconfigurado com classNames brandbook (border-left semântica + bg-ivory-100 + shadow-warm-md, sem richColors verde-neon/vermelho-frio), 4 toasts elevados para estado cúmplice (ação + consequência financeira: "Despesa lançada. Já entrou no fluxo de caixa." / "Meta salva. O dashboard mostra quanto falta a cada dia." / "Paciente cadastrado. Agora você pode agendar atendimentos." / "Despesa atualizada. O DRE deste mês foi recalculado."), anti-padrão "Use o botão acima" removido. **Achado importante da auditoria brand.4:** zero anti-padrões de voz ("amiga", "incrível", "destravar", "rumo ao sucesso", "estamos com você") no código — sistema já estava em tom mentora controlado antes da story. Validação técnica de cada PR: typecheck verde, lint zero warnings, build verde (38 rotas), RSC Boundary Audit PASS, Cross-tenant isolation PASS, Vercel deploy SUCCESS. **4 stories restantes:** brand.5 Motion + Framer (L, paralelizável), brand.6 Auth flows (M, todas dependências satisfeitas), brand.7 27 telas autenticadas (XL, todas dependências satisfeitas), brand.8 Remotion setup (S, paralelizável). **Próximo recomendado:** brand.5 (impacto perceptível imediato com page transitions + KPI reveal narrativo) ou brand.7 (sweep das telas, pode quebrar em sub-stories). **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-07 (continuação) — 🎉🎉🎉🎉 **EPIC-BRAND-INTEGRATION 2/8 stories Done · brand.1 (Foundation) + brand.2 (Components/keyra) MERGEADAS em main.** PR #13 squash mergeado (`d16f77b`); CI verde, Vercel deploy SUCCESS. **Story brand.2 entregou:** KPICard com container queries `clamp(28px,13cqi,44px)` + warm shadow + motion tokens; ComparativoTexto em Fraunces font-light italic (sussurro voz mentora); EmptyState com título Fraunces text-xl medium (editorial); AlertCard com border-left semântica + bg-ivory-100 (substitui bg-amber-50/red-50 frios por palette KEYRA warm); StatusBadge com 24 cores Tailwind defaults migradas (`bg-blue-100/text-blue-900` → `bg-amber-300/30 + text-cocoa-800`, `bg-secondary-100/text-secondary-700` → `bg-success-leaf/20 + text-success-deep`). **Regression fix incluído:** escalas `primary.{50..900}` e `secondary.{50..900}` re-adicionadas no Tailwind config mapeadas para palette KEYRA (preserva 24 referências legadas em button.tsx/page.tsx/LegalDocument). Validação técnica completa: typecheck verde, lint zero warnings, build verde (38 rotas), RSC Boundary Audit PASS, Cross-tenant isolation PASS. **6 stories restantes (paralelizáveis após Foundation):** brand.3 Components/ui shadcn (M), brand.4 Voice migration (M), brand.5 Motion + Framer (L), brand.6 Auth flows (M, depende 1-4), brand.7 27 telas (XL, depende 1-6), brand.8 Remotion (S). **Próximo:** abrir brand.5 (motion percebido imediatamente) ou brand.4 (voice migration nos toasts) ou brand.7 (sweep das telas). **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-07 — 🎉🎉🎉 **EPIC-BRAND-INTEGRATION ABERTO + Story brand.1 (Foundation) MERGEADA em main.** PR #12 squash mergeado (`7c0fafd`); Vercel prod build verde com tokens KEYRA canônicos ativos. Brandbook canônico v1 entregue em `docs/brand/03-identity/` (preview.html executável + PREVIEW-REFERENCE.md mapa canônico + BRANDBOOK + DESIGN + tokens.json + colors-manual + typography + voice-tone + motion-system com 6 docs + 4 templates Remotion + 12 referências visuais + 6 logo concepts primários + 6 sistema signature do ponto). **Foundation aplicada em apps/web:** `layout.tsx` carrega Fraunces (axes opsz+SOFT) + Inter via `next/font/google`; `globals.css` recebeu 25 cores canônicas KEYRA (--keyra-* prefix) + motion vars (durations, easings, distances, staggers) + warm shadows + premium-glow + dark mode editorial (cocoa-900 + champagne-200 + gold); `tailwind.config.ts` ganhou `fontFamily.serif`, palette KEYRA expandida (ivory/sand/mocha/bronze/cocoa/ink/amber/terracotta/rust/champagne/gold/success-leaf), `transitionDuration`, `transitionTimingFunction`, warm shadows, e animations `fade-up`/`fade-scale`. Tokens financeiros atualizados (lucro #6E8C5A oliva, prejuizo #843E1A rust, alerta #C8843A âmbar). Mapping legacy → canônicos preservado para compatibilidade shadcn. **Validação técnica:** typecheck verde, lint zero warnings, build verde (38 rotas), G5 RSC Boundary Audit PASS, Cross-tenant isolation PASS, Vercel deploy SUCCESS. **Lei da Densidade Proporcional** estabelecida como regra inegociável (DESIGN.md §9.bis): zero órfãos em qualquer grid, tabela canônica de cols por item count + breakpoint, CSS anti-órfão obrigatório. **Plano de 8 stories ordenadas:** brand.1 Foundation ✅ Done · brand.2 Components/keyra (L) · brand.3 Components/ui shadcn (M) · brand.4 Voice migration (M) · brand.5 Motion + Framer (L) · brand.6 Auth flows (M) · brand.7 27 telas autenticadas (XL) · brand.8 Remotion setup (S). **Próximo:** abrir story brand.2 (Components/keyra com KPI container queries + reveal narrativo) ou brand.3 (shadcn override) — paralelizáveis pois ambas dependem só da Foundation. **Pode encerrar terminal com segurança.**

> **Data anterior:** 2026-05-06 (encerramento de sessão) — 🎉🎉🎉 **EPIC-AUTH-V2 FECHADO PARCIALMENTE — 9/10 stories Done (90%) em prod. `auth.6` (Google OAuth) deferida pendente setup manual no Google Cloud Console pelo Luiz.** Esta sessão entregou em prod: `auth.5` (recovery com cooldown + signOut global + template recovery pt-BR) + `auth.8` (BroadcastChannel cross-tab) + `auth.9` (jornada afunilada com JourneyProgress + indicador força senha + tela /redefinir-senha/sucesso + visual revamp AppShell — bolha "K" Sidebar, header gradient sutil, UserMenu avatar bg-primary). **🔴 P0 fix descoberto via fiscalização E2E real:** usuário REAL caía em `/login?error=invalid_code` ao clicar no email de recovery (Supabase emite hash fragment, não chega no server) — corrigido com **token_hash flow** (Supabase recommended pra SSR, cross-device safe) + callback handler usando `verifyOtp({type, token_hash})` server-side. Smoke real validado pelo Luiz em mobile com email real chegando: 4 telas da jornada confirmadas funcionais + senha trocada com sucesso + login com nova senha funciona. **PRs mergeadas hoje:** #8 (auth.5 inicial, `5d9e71d`) + #9 (auth.8, `c54d443`) + #10 (P0 fix token_hash + smokes E2E reais, `f5256ba`) + #11 (auth.9, `3e3ab97`). **Migration 031 em prod:** `password_reset_attempts` + RPC cooldown + 4 deny-all policies. **Template recovery v2.0 em prod** com identidade KEYRA. **Próximo (em outra sessão):** ou rodar `auth.6` Google OAuth (após Luiz fazer setup Google Cloud — instruções em [`docs/setup/google-oauth-setup.md`](setup/google-oauth-setup.md)) **OU** abrir Phases 5-7 Pós-MVP (precificação inteligente, projeções, OCR/Asaas/WhatsApp). **Pode encerrar terminal com segurança** — todos os documentos sincronizados.

> **Data anterior:** 2026-05-06 — 🎉🎉 **STORY `auth.8` Done na sequência da `auth.5` (8/10 do EPIC-AUTH-V2 — 80%).** Fix do bug de 2 abas via `BroadcastChannel` API: aba antiga em `/esqueci-senha` agora detecta automaticamente quando outra aba completa o reset e exibe CTA "Ir para o login". Mudança cirúrgica em 4 arquivos: 1 helper novo (`lib/auth/broadcast.ts` com defensive no-op em SSR/browsers antigos) + 1 action ajustada (`setNewPasswordAction` retorna `{success:true}` em vez de `redirect()` server-side, para liberar broadcast antes da navegação) + 2 cards (`NewPasswordCard` posta evento + `router.push`; `RequestResetCard` escuta com `useEffect` + cleanup, estado `completedElsewhere` toma precedência sobre `submitted`). Sem schema/envs/Management API — story XS de 1 ponto. Quality gates verdes: `pnpm typecheck` ✅ · `pnpm lint --max-warnings 0` ✅ · `./scripts/check-rsc-boundaries.sh` PASS · QA self-gate 7/7 PASS. Branch `feat/auth-v2-story-8` (commit `29c8e7e`) pendente push + merge + deploy. **Fase C agora só tem `auth.6` (Google OAuth) restante; Fase D só `auth.9` (visual revamp AppShell).**

> **Data anterior:** 2026-05-06 — 🎉 **STORY `auth.5` Done em prod (7/10 do EPIC-AUTH-V2 — 73%).** Migration `20260506000100_password_reset_attempts` aplicada via `supabase db push` com smoke transacional verde via psql (table_exists ✅, rls_enabled+forced ✅, rpc_exists ✅, 4 policies ✅, RPC primeira=true / segunda=false (cooldown ativo) / outro-email=true). Template recovery pt-BR aplicado via Management API com snapshot defensivo + validação GET — `mailer_subjects_recovery="Redefinir sua senha do KEYRA"` + 6925 chars HTML identidade KEYRA. **6 riscos da auditoria mitigados nesta story:** R3 Turnstile · R8 anti-enumeration · R11 signOut global · R12 otp 30min · R14 cooldown 60s server-side · R16 Sentry breadcrumb sem PII. Branch `feat/auth-v2-story-5` com 17 arquivos / +1683 linhas commitada localmente; PR pendente push. Quality gates verdes: `pnpm typecheck` ✅ · `pnpm lint --max-warnings 0` ✅ · `./scripts/check-rsc-boundaries.sh` PASS · QA self-gate 7/7 PASS. **Próximo:** push do PR + smoke E2E real da idealizadora (clicar link de email + redefinir senha) + abrir `auth.6` (Google OAuth) ou `auth.8` (BroadcastChannel) ou `auth.9` (visual revamp AppShell).

> **Data anterior:** 2026-05-04 — 🎉🎉🎉🎉 **EPIC-AUTH-V2 com 6 stories Done em prod + tela light KEYRA.** PR #7 mergeada (`14ec8e3`) + redeploy `5ad5805` → `usekeyra.com` agora com tela `/login` email+senha, `/cadastro` estruturado e `/esqueci-senha` placeholder, todas no estilo HextaUI (card centralizado, animações hover) com cores light KEYRA (cream/bege + primary marrom + cards brancos). Magic link **100% removido** da plataforma. **Stories Done:** auth.0 (foundation segurança) + auth.1 (schema profiles+consent+legal_documents+hook update) + auth.2 (Termos+Privacidade v1.0.0) + auth.3 (cadastro estruturado) + auth.4 (login email+senha) + auth.7 (full_name no AppShell). **6 migrations em prod:** 026, 026.5, 027, 028, 029, 030. **Senha temporária `Keyra2026Test!`** setada via Admin API pros 2 users existentes. **Próximo:** auth.5 (resetPasswordForEmail — recovery sem magic link) + auth.6 (Google OAuth) + auth.8 (BroadcastChannel) + auth.9 (visual revamp AppShell autenticado pra coerência). **Encerramento da sessão 2026-05-04:** todos os documentos atualizados, ADR-010 marcado como SUPERSEDED por ADR-022, mapa de atualização em `docs/sessions/2026-05-04-session-update-map.md`. Pode encerrar com segurança.

> **Data anterior:** 2026-05-04 — 🎉🎉🎉 **EPIC-AUTH-V2 Fase B MERGEADA em main + DEPLOY PROD READY.** PR #5 squash mergeada (`5cd4900`); Vercel prod READY; usekeyra.com com tela de login email+senha (sem magic link), página /cadastro estruturada com 8 campos, /termos e /privacidade públicas servindo conteúdo v1.0.0 do banco, /esqueci-senha placeholder com suporte. **6 stories Done do EPIC-AUTH-V2** (auth.0 + auth.1 + auth.7 + auth.2 + auth.3 + auth.4). 5 migrations em prod (026, 026.5, 027, 028, 029, 030). Smoke E2E pós-merge: ✅ /login email+senha sem magic link, ✅ /cadastro carrega com nome+celular+email+senha+conf+nome clínica+CNPJ+aceite Termos, ✅ /termos com 6171 chars renderizando, ✅ /privacidade com 7575 chars renderizando, ✅ /esqueci-senha placeholder, ✅ /dashboard 307 redirect sem auth. **Senha temporária `Keyra2026Test!` setada via Admin API** pros 2 users existentes do Luiz (luizzzhenriqueoficial@gmail.com + luizxhenriquepro@gmail.com) — pode logar IMEDIATAMENTE com email + senha temporária. **Próximo:** auth.5 (resetPasswordForEmail — recovery sem magic link) + auth.6 (Google OAuth) + auth.8 (BroadcastChannel pra fluxo de recovery) + auth.9 (visual revamp completo). Magic link foi REMOVIDO INTEIRO da plataforma por decisão estratégica da idealizadora.

> **Data anterior:** 2026-05-04 — 🎉🎉 **STORY `auth.1` MERGEADA em main + DEPLOY PROD READY.** PR #4 squash mergeada (`eaa3520`); Vercel prod deploy READY em ~90s; usekeyra.com atualizado. EPIC-AUTH-V2 com 2 stories Done (`auth.0` foundation + `auth.1` schema). 3 migrations em prod (026, 026.5, 027) + hook custom_access_token_hook ATIVA emitindo `{org_id, full_name}` no claim — validado em prod com user real (`luizzzhenriqueoficial@gmail.com` → claim `{"org_id":"b4d01272-...","full_name":"Luiz Henrique"}`). Smoke E2E pós-merge: `/login` 200, `/dashboard` 307 redirect. Zero erros em audit_log. Gates Phase 3.5: `@data-engineer` 14/14 + `@compliance-br` 10/10. QA Quinn 7/7. CI checks 4/4 PASS (RSC audit 20s + Cross-tenant isolation 48s + Vercel + Comments). 4 commits squashed em 1. Branch deletada. Backend pronto para `auth.3` (cadastro com formulário) e `auth.4` (login email+senha) consumirem. **Próximo:** Fase B+ do EPIC — `auth.2` (textos legais Termos+Privacidade) + `auth.4` (login email+senha — TELA DE LOGIN MUDA AQUI) + `auth.7` (consumo do claim full_name no AppShell — "Olá, Luiz Henrique" aparece aqui).

> **Data anterior:** 2026-05-03 — 🎉 **STORY `auth.0` Done — em produção (`usekeyra.com`).** PR #3 aberta com 14/14 conformidades verificadas, todos os CI checks PASS, squash merge via `gh pr merge 3 --squash` resultou em `dbc753e` em main. Vercel prod deploy READY em ~75s. Foundation de segurança do EPIC-AUTH-V2 ATIVA EM PRODUÇÃO: Sentry PII scrubbing redactando `password|senha|cpf|phone|token|Authorization` antes de sair pra Sentry US; Cloudflare Turnstile envs provisionadas em Vercel nos 3 targets prontas pra `auth.3-6`; Supabase Auth de produção endurecida (mailer_autoconfirm=false, password_min_length=10 + lower+upper+digits, security_update_password_require_reauthentication=true, mailer_otp_exp=1800, rate_limit_email_sent=30, refresh_token_rotation_enabled=true) — 7/7 configs validadas via GET. Branch `feat/auth-v2` deletada (local + remote). Story Status Ready for Review → Done com gate `@compliance-br` APPROVE + 2 CONCERNS propagados como dependências de `auth.2`. **Próximo:** abrir Fase B do EPIC-AUTH-V2 — `auth.1` (schema profiles+consent_records+legal_documents+auth hook estendido) + `auth.2` (textos legais Termos+Privacidade) + `auth.4` (login email+senha) + `auth.7` (custom claim full_name no JWT) podem rodar em paralelo agora que a foundation está em main.

> **Data anterior:** 2026-05-03 — 🟢 **STORY `auth.0` Ready for Review. Foundation de segurança do EPIC-AUTH-V2 entregue ponta-a-ponta.** Cloudflare Turnstile widget criado pela idealizadora (`keyra`, hostnames `usekeyra.com`+`localhost`); secret validada via API Cloudflare antes de provisionar; envs `NEXT_PUBLIC_TURNSTILE_SITE_KEY`+`TURNSTILE_SECRET_KEY` provisionadas via Vercel REST API nos 3 targets (Production+Preview+Development); Sentry scrubbing server+client com selftest verde redacta `password|senha|cpf|phone|token|Authorization`; helper `verify-turnstile.ts` server-only com bypass dev controlado e fail-hard em prod; **script `configure-supabase-auth-prod.sh` rodado em prod com sucesso** — 7/7 configs universais aplicadas e validadas via GET (`mailer_autoconfirm=false` R1, `password_min_length=10` + complexidade R2, `security_update_password_require_reauthentication=true` R11, `mailer_otp_exp=1800` R12, `rate_limit_email_sent=30` R20, `refresh_token_rotation_enabled=true` R22 parcial). **Descoberta operacional crítica:** Supabase Management API retorna HTTP 200 silenciosamente ignorando campos com naming errado — naming real exposto pelo GET (`security_update_password_*` não `secure_password_*`; `sessions_*` plural não `session_*` singular). Script foi atualizado com naming canônico + validação GET integrada como defesa contra silent-drop. Memory `feedback_supabase_patch_silent_drop.md` registrada. `sessions_timebox`+`sessions_inactivity_timeout` requerem Pro Plan (HTTP 402) — R22 fica parcialmente mitigado, deferido com Vercel Pro. ADR-022 publicado em ARCHITECTURE.md §11.2. Typecheck ✅ + lint ✅ + RSC audit ✅. Branch `feat/auth-v2` (commit `0a34167` em preview Vercel READY); commit final consolidado pendente.

> **Próximo:** gate `@compliance-br` revisa PII scrubbing + email confirmation + CAPTCHA + (futuro) termos. Após review, abrir PR pra merge em main → libera Fase B (auth.3 cadastro, auth.4 login senha, auth.7 custom claim) executando em paralelo. Antiga Sprint 8 hardening (h8.*) congelada — vira Sprint 9 quando Auth V2 fechar.

> **Data anterior:** 2026-05-02 — 🔴→🟢 **SESSÃO HOTFIX RSC: bugs em sequência apareceram em produção real e foram corrigidos + reforço estrutural anti-regressão definitivo.** Idealizadora abriu produção autenticada e descobriu que `(app)/*` quebrava com digest `3213099672` em TODAS as rotas exceto Agenda — apesar de Sprint 7 ter sido fechada como "Done" 4 dias antes com deploys READY. Causa raiz NÃO era hidratação genérica (que era a hipótese da Sprint 7); era violação sistemática de fronteira RSC que TypeScript+ESLint+build+QA gate NÃO pegam: (R1) `forwardRef` (Lucide icons, shadcn/ui Card/Button) passado como prop através de Server↔Client, (R2) Client importando Server, (R3) `useSyncExternalStore` com SSR snapshot inconsistente em Next 16. **Sequência de fixes em produção:** commit `d39c84d` (EmptyState Server) → `7f2a7ef` (AlertasList InlineEmptyState) → `4853c53` (useDismissedAlerts sem useSyncExternalStore) → `742b3f2` (cleanup + 4 regras RSC documentadas) → `f5abbdb` (renames Pacientes→Clientes/Comandas→Serviços/Serviços→Catálogo + Sidebar sticky+colapsável + KPI tokens reduzidos + script audit + CI gate). Reforço estrutural definitivo (commit final desta sessão): script `scripts/check-rsc-boundaries.sh` resolve imports reais para detectar Server passando Lucide para Client (sem falsos positivos Server→Server), CI workflow `rls-tests.yml` ganha job `rsc-audit` que bloqueia PR se falhar, `.claude/CLAUDE.md` + `story-lifecycle.md` G5 tornam leitura de `docs/dev/rsc-boundary-rules.md` obrigatória. **Lição operacional incorporada:** build verde / QA PASS / deploy READY ≠ produto funcional. Smoke real da idealizadora em mobile autenticado é critério de Done não negociável.

> **Data anterior:** 2026-05-02 — 🟢🟢 Sprint 7 ENCERRADA (declarada Done sem smoke real — origem dos bugs RSC desta sessão). Após sequência de correções de código (4 commits em main, todos READY na Vercel) que desbloquearam todas as rotas `(app)/*` (root cause: `<m.div className="contents">` em template.tsx), uma rodada subsequente de hardening fechou as 3 dependências não-código que sobravam: **(A) Supabase magic-link template em pt-BR aplicado via Management API** (`PATCH /v1/projects/{ref}/config/auth` com `mailer_subjects_magic_link` + `mailer_templates_magic_link_content`) — daqui pra frente magic link sai com identidade KEYRA, subject "Seu link de acesso ao KEYRA"; **(B) Sentry production capture 100% funcional** — descobri que envs `SENTRY_ORG`/`SENTRY_PROJECT`/`SENTRY_AUTH_TOKEN` estavam mal-configuradas (slug invertido, trailing newline, token formato legado de 32 hex chars que retornava 401). Corrigido: `SENTRY_ORG=seal-digital`, `SENTRY_PROJECT=keyra` (slugs limpos sem newline), novo `SENTRY_AUTH_TOKEN` formato `sntrys_*` validado via API antes de provisionar. Source maps subindo automaticamente (release `1cfb1773` confirmado no log do build); **(C) Repo `luizxhgomes/keyra` ↔ Sentry GitHub integration** verificado ativo via `GET /api/0/organizations/seal-digital/repos/` — `status=active provider=integrations:github`. **Único pendente real:** validação manual em mobile da idealizadora confirmando que `/dashboard` & cia carregam sem digest `3213099672`. Tudo o mais (código, deploys, observabilidade, branding email, integração GitHub↔Sentry) está em conformidade.

> **Data anterior:** 2026-05-02 — 🚨 Sprint 7 ATIVADA após validação manual descobrir 4 issues HIGH+CRITICAL.

> **Data anterior:** 2026-05-02 — 🎉🎉 **SPRINT 6 FECHADA — 100% (25/25 pts). DS editorial completa, Motion + Painel visual interativo, Navegação contextual.** Phases 1+2+3+4 = 100% + Sprint 5 + 5.7 + Sprint 6 (6.0+6.1+6.2+6.3+6.4+6.5). **Story 6.5 (Sidebar 7+2 + FAB contextual)** fechada — Sidebar reorganizada em 7 primários (Dashboard, Agenda, Comandas, Pacientes, Serviços, Financeiro, Estoque) + 2 secundários (Time, Configurações) separados por `border-t`; BottomNav FAB ganhou função pura `getFabAction(pathname)` exportada com 6 mapeamentos contextuais (paciente/serviço/despesa/insumo/comanda) + fallback `/agenda?novo=1`. **Phase 2.5 segunda story a herdar a regra com gates limpos** — sem pre-work cirúrgico, story enxuta (S 3 pts) confirmada como contraponto da 6.2 (M+ 6 pts com pre-work pesado). Os 4 gates G1-G4 não acrescentam — diagnosticam complexidade real. Anti-grep G1 (toFixed(N)%) zerado em layout. **Próxima ação**: considerar Story 6.2.1 (deferred ACs 2.3 stagger + 2.5 layoutId) ou avançar para Pós-MVP (Phases 5-7).
> **Última entrega:** **Sprint 3 fechada** — todas as 8 stories de Automação Financeira (3.1-3.8) entregues em uma sessão com QA gate PASS. O ciclo do diferencial KEYRA está vivo end-to-end: `criar agendamento → concluir → comanda automática → pagamento (com cálculo de fee) → transação automática → rateio de estoque`. Migrations 024 (`seed_chart_on_org_create` que faz seed das orgs novas + backfill nas existentes) e 025 (`professionals.cost_center`) aplicadas. UI nova: `/comandas`, `/comandas/[id]` (com PaymentsCard + ConsumoCard), `/financeiro/transacoes`, `/financeiro/receitas`, `/financeiro/despesas` (CRUD), `/financeiro/custos-fixos` (com clone do mês passado), `/financeiro/fluxo-caixa` (saldo + tabela diária + projeção), `/financeiro/categorias`. **Decisão de arquitetura central: zero cálculo na aplicação** — todos os totais são mantidos por triggers (`trg_command_items_recompute`, `trg_payments_to_transaction`, `_consume_command_inventory`) e GENERATED columns. App só dispara INSERT/UPDATE/DELETE; banco cuida do resto. RLS dupla (filtro explícito + security_invoker da migration 023). Smoke transacional via psql + ROLLBACK validou todos os 3 ciclos críticos (comanda gerada, pagamento → transação, rateio de insumos).
>
> **Próxima ação recomendada:** **Sprint 4 — Lucro + Dashboard** (~38 pts, fecha o MVP). 9 drafts criados pelo @sm, todas validadas pelo @po (GO 9-10/10) e Ready: 4.1 DRE básica · 4.2 DRE por serviço (diferencial vs Conta Azul) · 4.3 Lucro por profissional · **4.4 Dashboard tela única ⭐ (story-anchor)** · 4.5 Agenda do dia · 4.6 Indicadores · 4.7 Comparativo mês vs anterior · 4.8 Comparativo vs meta · 4.9 Alertas. Recomendação tática: começar pela **4.4** (vitrine + slots para 4.5-4.9) e construir as outras encaixando no dashboard.
>
> Este documento é a **entrada única** para retomar o contexto do KEYRA em qualquer nova sessão. Foi projetado para ser lido em menos de 60 segundos. **Regras operacionais:**
> 1. Este snapshot é atualizado ao fim de cada Story (status Done) — header + tabela de status macro + §3 lista da sprint atual + §6 próxima ação + §8 Histórico.
> 2. **Validação de jornada de usuário** é critério de Done a partir da Sprint 6 — toda story precisa garantir que a persona Camila (`docs/ux/user-journeys.md`) consegue executar a jornada que a story toca, em viewport 375px, sem documentação. Não basta typecheck/lint/build verde.

---

## 1. Status Macro

| Camada | Progresso | Detalhe |
|--------|-----------|---------|
| **Infraestrutura** | ✅ 100% | GitHub `luizxhgomes/keyra` · **Vercel domínio canônico `https://usekeyra.com`** (Hobby; alias `usekeyra.vercel.app` continua respondendo) · Supabase `keyra-br` sa-east-1 (Free) · Auth Hook ativo · `COLUMN_ENCRYPTION_KEY` provisionada · `RESEND_API_KEY` + `EMAIL_FROM` + `NEXT_PUBLIC_SITE_URL=https://usekeyra.com` em Production/Preview do Vercel · Resend `usekeyra.com` **Verified** (DMARC, DKIM, SPF, MX) · SMTP customizado do Supabase Auth apontando para Resend |
| **Documentação (Phase 0)** | ✅ 100% | PRD v1.3 · ARCHITECTURE v1.3 (20 ADRs) · SCHEMA (21 tabelas · 100% RLS · 6 views · 15 funções · 19 migrations) · 8 wireframes · 6 pesquisas competitivas |
| **Phase 1 — Fundação Técnica** | ✅ **100% Done + validado em prod** | 1.1 ✅ · 1.2 ✅ · 1.3 ✅ · 1.4 ✅ · 1.5 ✅. PR #1 mergeado em `main`. Migrations 100% sincronizadas (22/22 no remoto), `database.types.ts` regenerado, suíte RLS verde no CI, Resend Verified, fluxo de convite **end-to-end testado em produção**: convidado recebe email da marca KEYRA, clica, autentica via magic link, retorna ao convite (graças ao fix do `?next=`), aceita e cai no dashboard da clínica. |
| **Phase 2 — Catálogo + Agenda** | ✅ **100% (em pontos)** | Stories 2.1 e 2.2 em InReview · **Story 2.3 (Insumos + BOM) ✅ Done** · **Story 2.4 (Agenda FullCalendar) ✅ Done** · **Story 2.5 (Agendamento) ✅ Done** · **Story 2.6 (Status) ✅ Done** + smoke empírico validado · **Story 2.7 (Receita prevista) ✅ Done** + migration 023 (security_invoker em todas as views). Em pontos: 28 de 28 entregues (10+7+3+2+6 de 6+10+7+3+2). **Concerns C1+C2 zerados.** Próximo: Sprint 3 (Automação Financeira). |
| **Phase 3 — Automação Financeira** | ✅ **100% (32/32 pts)** | 8 stories Done. Ciclo do diferencial KEYRA vivo end-to-end. Migrations 024+025. |
| **Phase 4 — Lucro + Dashboard** | ✅ **100% (~38 pts)** | **9 stories ✅ Done em uma sessão**: 4.1 DRE básica · 4.2 DRE por serviço (diferencial) · 4.3 Lucro por profissional · 4.4 Dashboard tela única · 4.5 Agenda do dia · 4.6 Indicadores · 4.7 ComparativoTexto · 4.8 Metas · 4.9 Alertas. **MVP FEATURE-COMPLETE.** |
| **Sprint 5 — Higienização visual** | ✅ **100% (~28 pts)** | 5.1-5.7 todas Done. Score saiu de 6.8 para alvo 8.0+. |
| **Sprint 6 — Editorial + Motion** | ✅ **100% (25/25 pts)** | **Sprint 6 FECHADA.** 6.0 + 6.1 + 6.2 + 6.3 + 6.4 + 6.5 Done. DS editorial + Motion + Painel visual interativo + Navegação contextual. Phase 2.5 Anti-Regression Gates instaurada. AC2.3 e AC2.5 deferidos para Story 6.2.1. |
| **Sprint 7 — Auth UX & Reliability** | 🟢🟢 **ENCERRADA — código + observabilidade + branding email ativos** | **7.0 ✅** (Sentry capture) · **7.0.1 ✅** (`withSentryConfig` + source maps) · **7.0.2 ✅ HOTFIX** (template.tsx passthrough — root cause `<m.div className="contents">` UB) · **7.1 ✅** (login UX: cooldown 60s + "Reenviar link") · **7.2 ✅** (template magic-link pt-BR aplicado via Supabase Management API) · **7.3 ✅** (`/mais` + `/configuracoes`) · **7.4 ✅** (3× not-found.tsx + global-error.tsx + Phase 2.6 Gate G5) · **7.5 ⏭️** (coberta — `(app)/error.tsx` mantém AppShell) · **7.6 ⏸️** (home cleanup opcional). Commits: `8ab06b1` → `58988d4` → `02ec96f` → `0f8888f` → `1cfb177`. Vercel envs Sentry corrigidas (`SENTRY_ORG=seal-digital`, `SENTRY_PROJECT=keyra`, novo `SENTRY_AUTH_TOKEN` `sntrys_*`). Source maps subindo. Repo `luizxhgomes/keyra` ↔ Sentry GitHub `status=active`. Todos deploys produção READY. **Único pendente:** validação manual em mobile da idealizadora. |
| **EPIC-AUTH-V2 — Auth UX V2 + LGPD** | 🟢 **90% — 9/10 stories Done (41/47 pts) · auth.6 deferida** | Fase A (auth.0+1) ✅ · Fase B (auth.2+3+4+7) ✅ · Fase C (auth.5 ✅ + auth.8 ✅ · auth.6 ⏸️ **deferida**) · Fase D (auth.9 ✅). Migration 031 + template recovery v2.0 (token_hash flow cross-device safe) em prod. P0 fix do callback handler validado E2E real 12/12 PASS contra usekeyra.com. Story auth.9 entregou jornada afunilada (JourneyProgress 4 fases) + indicador força senha + tela `/redefinir-senha/sucesso` + visual revamp AppShell (Sidebar bolha "K", header gradient terracota, UserMenu avatar bg-primary). **`auth.6` (Google OAuth)** aguarda setup manual Google Cloud Console pelo Luiz — guia completo em [`docs/setup/google-oauth-setup.md`](setup/google-oauth-setup.md). Não bloqueia mais nada. |
| **EPIC-COMPROVANTES — Comprovantes Inteligentes (Phase 7.0)** | 📋 **Planejamento mergeado — 7 stories Ready (~48 pts)** | PR #34 (`1d7bac8`) em `main`: ADR-023 + spec rev. 2 + epic + Política v1.1.0 DRAFT + auditoria LGPD (CONCERNS endereçável) + 2 pareceres + 7 stories `comprovantes.0`–`.5` validadas `@po` GO 10/10. Captura de comprovante em qualquer formato → leitura por LLM (`gpt-4o-mini` via AI Gateway) → revisão humana → `transactions`. Implementação ainda não iniciada; 2 spikes obrigatórios (`.1` RLS Storage, `.2` rasterização PDF). `comprovantes.3` bloqueada para prod até `.0` Done (Política v1.1.0 + re-aceite forçado). Próximo: `@dev *develop-story comprovantes.1`. |
| **Phases 5–7 — Pós-MVP** | ⏸️ 0% UI | Phase 5 = Precificação inteligente, pacotes, alertas de recompra, Stripe. Phase 6 = Projeções, prontuário financeiro. Phase 7 = OCR, Asaas Pix, WhatsApp, NFS-e. |
| **Testes automatizados** | 🔴 0 | Nenhum arquivo `.test.*` ou `.spec.*` no repo. Validação manual + typecheck + lint + build apenas |

---

## 2. O Que Já Funciona em Produção

Rodando hoje em `usekeyra.vercel.app`:

- **Autenticação passwordless** — magic link com Zod + Sonner, callback `exchangeCodeForSession`, `proxy.ts` (renomeado do `middleware.ts` em Next 16) que protege rotas `(app)/*` e redireciona `/login` se já autenticado
- **Onboarding da 1ª organização** — formulário com CNPJ mascarado, RPC `create_org_with_owner` que resolve o chicken-and-egg do RLS, compensating delete em caso de falha
- **Org switcher** — dropdown quando há ≥ 2 memberships, texto puro quando há apenas 1; troca com refresh de JWT
- **User menu** — avatar + sign-out server action
- **AppShell** — Sidebar 240px no desktop + BottomNav de 5 itens no mobile; `requireAuth()` no layout `(app)`
- **Infra de código** — Supabase clients (`server`/`browser`/`middleware`/`admin` com `server-only`), Decimal.js (`ROUND_HALF_EVEN`), date-fns pt-BR, Sentry auto-init, Zod env validation, ESLint flat + Prettier, **Resend + React Email** (ADR-021)
- **Dashboard placeholder** — grid com componentes canônicos (`KPICard`, `ComparativoTexto`, `AlertCard`, `StatusBadge`) mostrando dados mockados — será substituído por queries reais na Story 4.4

## 2.1. Aceite da Phase 1 (sessão 2026-04-30)

Verificações executadas e resultado:

| # | Item | Status | Evidência |
|---|------|--------|-----------|
| 1 | Migration 022 (`accept_organization_invite` + `count_org_owners`) aplicada no remoto | ✅ | `supabase migration list --linked` mostra 22/22 em sync |
| 2 | `database.types.ts` regenerado com schema atual | ✅ | 1812 linhas, 54 KB, gerado em 2026-04-20 16:50 |
| 3 | Nenhum cast `as never` em `team/actions.ts` | ✅ | `grep "as never"` retorna vazio |
| 4 | Suíte RLS verde no CI | ✅ | PR #1 mergeado em `main` com workflow `rls-tests.yml` aprovado |
| 5 | `RESEND_API_KEY` + `EMAIL_FROM` no `.env.local` | ✅ | `re_***[masked]***` + `KEYRA <no-reply@usekeyra.com>` (raiz e `apps/web/`) |
| 6 | Typecheck (`pnpm typecheck`) | ✅ | zero erros |
| 7 | Lint (`pnpm lint --max-warnings 0`) | ✅ | zero warnings |
| 8 | Domínio `usekeyra.com` no Resend | ✅ **Verified** | Re-verificou sozinho após NS Hostinger → Vercel; 5 registros confirmados (`send` SPF, `send` MX, `resend._domainkey`, `_dmarc`, MX inbound apex) |
| 9 | Smoke test manual em **dev** (1.3 + 2.1 + 2.2) | ✅ | Login + onboarding + criação de pacientes/serviços OK; convite enviado para `luizxhenriquepro@gmail.com` chegou na inbox principal com remetente `KEYRA` e template renderizado |
| 10 | Provisionar `RESEND_API_KEY` + `EMAIL_FROM` no Vercel (3 targets) | ✅ | Adicionados via CLI (Production/Development) e REST API (Preview, devido a bug do CLI 50.38.x). Também `NEXT_PUBLIC_SITE_URL=https://usekeyra.com` em Production+Preview |
| 11 | SMTP customizado no Supabase Auth | ✅ | `smtp.resend.com:587` com sender `KEYRA <no-reply@usekeyra.com>` — magic link agora sai pela marca, não mais como `Supabase Auth` |
| 12 | Bug fix: `?next=` propagado pelo magic link inteiro | ✅ | Helper `lib/auth/safe-next.ts` (anti open-redirect) + 5 pontos da cadeia (`/login` page+form+action, `/auth/callback`, `proxy.ts`). Convite agora retorna ao `/invites/{token}` após autenticar — não cai mais em `/onboarding/nova-organizacao` |
| 13 | Smoke test do fluxo de convite **em produção** | ✅ | Email com link `https://usekeyra.com/invites/{token}` chegou ao destinatário; aceite confirmado |

### Mudanças desta sessão (a comitar)

- `apps/web/src/lib/env.ts` — default de `EMAIL_FROM` migrado de `keyra.app` para `usekeyra.com`
- `scripts/sync-env.sh` — passa a ler `.keyra-secrets/resend-api.key` e `.keyra-secrets/email-from.txt` (helper `read_line_or_empty` preserva espaço entre nome amigável e endereço)
- `.keyra-secrets/resend-api.key` (gitignored, chmod 600) — chave do Resend
- `.keyra-secrets/email-from.txt` (gitignored, chmod 600) — `KEYRA <no-reply@usekeyra.com>`

### Sprint 1 (Phase 1) — completo (entregue em 2026-04-20, aceito em 2026-04-30)

- **Story 1.3** — Módulo `/team` com três sub-páginas (membros, profissionais, convites), Server Actions completas (CRUD profissionais, convite com Resend + React Email, aceite via RPC `accept_organization_invite`, promover/rebaixar/remover membros com invariante de último owner), rota pública `/invites/[token]` com 5 estados (bogus, consumido, expirado, email diferente, happy), item "Time" no Sidebar.
- **Story 1.4** — Suíte RLS expandida cobrindo 21 tabelas + smoke inverso (checa `pg_class.relrowsecurity`), workflow GitHub Actions `.github/workflows/rls-tests.yml` rodando em push/PR com Postgres 17 service container, badge no README, `docs/testing/rls-tests.md`, atalho `scripts/run-rls-tests.sh` com autodetect de libpq via Homebrew (macOS).
- **Tech debt zerado** — `middleware.ts` → `proxy.ts` (Next 16), `database.types.ts` com as 21 tabelas + 6 views (script `pnpm typegen` adicionado).
- **ADR-021** — Resend + React Email como provedor transacional, documentado em `ARCHITECTURE.md` §11.1.
- **Migration 022** — `accept_organization_invite` + `count_org_owners` **aplicadas no Supabase remoto** via `supabase db push`.
- **Testes RLS locais** — passaram 100 % em Postgres 17 efêmero (docker).

### Sprint 2 (Phase 2) — em andamento

- **Story 2.1 — Pacientes** — Página `/pacientes` com lista paginada (20/pág.) + busca fuzzy via `customers_full_name_trgm`, filtro "mostrar arquivados", paginação. Criar `/pacientes/novo` e editar `/pacientes/[id]` em páginas dedicadas. Actions `upsertPatient`, `archivePatient`, `unarchivePatient` com Zod + requireRole('professional'|'admin'). Placeholder de histórico de atendimentos (Story 2.4 preenche).
- **Story 2.2 — Serviços + Categorias** — Módulo `/servicos` com sub-nav (Serviços · Categorias). Lista agrupada por categoria, filtro por tipo (service/product), busca fuzzy. Form com nome, tipo, categoria, preço (BRL), custo, comissão (%), duração. `/servicos/categorias` com CRUD dedicado (nome, cor hex, sort_order). Usa `useWatch` de react-hook-form para evitar warning do React Compiler.
- **Drafts prontos** — Stories 2.3, 2.4, 2.5, 2.6, 2.7 em `docs/stories/*.story.md` (Draft — validação pelo @po na próxima sessão).

---

## 3. O Que Falta Para Fechar o MVP

### Sprint 1 — fundação técnica

✅ **Concluído nesta sessão.** Pendente apenas os 7 passos de aceite listados em §6 (aplicar migration, typegen, validar testes, subir Resend, push).

### Sprint 2 — Cadastros e Agenda (~2 semanas)

2.1 Pacientes ✅ (InReview) · 2.2 Serviços ✅ (InReview) · 2.3 Insumos + rateio ✅ Done · 2.4 Agenda (FullCalendar dia/semana/mês) ✅ Done · 2.5 Agendamento ✅ Done · 2.6 Status ✅ Done · 2.7 Receita prevista automática ✅ Done

### Sprint 3 — Automação Financeira (~2 semanas) — *coração do diferencial*

3.1 Comanda automática (trigger `trg_appointments_done_to_command` já pronto no banco) · 3.2 Pagamento (Pix/cartão/dinheiro) · 3.3 Transação automática (trigger `trg_payments_to_transaction` já pronto) · 3.4 Receitas por profissional · 3.5 Despesas · 3.6 Custos fixos vs variáveis · 3.7 Fluxo de caixa · 3.8 Rateio de estoque

### Sprint 4 — DRE e Dashboard (~1.5 semana) — *fecha o MVP*

4.1 DRE básica · 4.2 **DRE por serviço** (diferencial anti-Conta-Azul) · 4.3 DRE por profissional · 4.4 Dashboard tela única · 4.5 Agenda do dia · 4.6 Indicadores (ticket médio, top serviço, no-show) · 4.7 Comparativo mês vs mês · 4.8 Comparativo vs meta · 4.9 Alertas

> **Prazo total para MVP feature-complete:** ~6,5 semanas a partir do próximo commit.

### Pós-MVP (Phases 5–7)

- **Phase 5** — Motor de precificação (BOM + margem), estoque com lote/validade, alertas de recompra, Stripe billing
- **Phase 6** — Projeções, what-if, rentabilidade por horário, prontuário financeiro, sugestões de upsell
- **Phase 7** — OCR de extratos e maquininhas, Asaas Pix, WhatsApp Business API, NFS-e

---

## 4. Bloqueadores de Decisão (precisam da idealizadora)

Nenhum impede a próxima Story (2.4 — Agenda); cada um trava uma Story específica mais à frente:

| # | Decisão pendente | Trava |
|---|------------------|-------|
| 1 | Validar paleta terracota na prática | Design do Sprint 2 em diante |
| 2 | Arquitetura de split de pagamento (Pix + cartão na mesma comanda) | Story 3.2 |
| 3 | Formato do comparativo textual: "R$ X a mais que Y" vs "↑ R$ X vs Y" | Story 4.7 |
| 4 | Pricing absoluto dos planos Start / Crescimento / Autoridade | Comercial / landing |
| 5 | Comissionamento por senioridade (hierarquia de `%`)? | Cadastro de profissionais |
| 6 | Fluxo financeiro de devolução / desconto | Comanda (Story 3.x) |

---

## 5. Risco Operacional Imediato

- **Supabase Free pausa após 7 dias sem requests.** Última atividade em 2026-04-30 (login + onboarding + smoke test em prod) — janela de pausa empurrada para ~2026-05-07. Mitigação preventiva: criar cron de healthcheck (curl simples no endpoint REST) já na Sprint 2, idealmente embarcado na Story de Agenda (que vai naturalmente bater no banco).
- **Plano Vercel Hobby** não permite uso comercial. Antes do primeiro pagante (Phase 5+) é obrigatório migrar para Pro.
- **Tech debt residual da Phase 1** (não bloqueia Sprint 2 mas vale registrar): warning recorrente do Supabase em todas as requests — `Using the user object as returned from supabase.auth.getSession() ... may not be insecure`. Ação: trocar `getSession()` server-side por `getUser()` em todos os entry points autenticados. Boa candidata para uma `1.6 — polish & hardening` ou para ser ratada na primeira story da Sprint 2 que tocar o auth path.

---

## 6. Próxima Ação Concreta — Luiz (sessão 2026-05-18 encerrada)

📋 **EPIC-COMPROVANTES planejado e mergeado.** Toda a Phase 7.0 (Comprovantes Inteligentes) foi planejada, revisada por especialistas, validada pelo `@po` e mergeada em `main` via PR #34. 7 stories `Ready`, ~48 pontos. Nada de código de aplicação ainda — só planejamento.

**Próxima ação concreta: `@dev *develop-story comprovantes.1`** — fundação de schema (tabela `receipts` + bucket Storage privado + RLS + trigger `audit_receipts` + micro-migration `transactions.source_type`), começando pelo **spike obrigatório de RLS de Storage** (se VERMELHO, para e escala para `@architect`/`@data-engineer`). Ordem da cadeia: `.1` → `.2`/`.3` → `.4a` → `.4b` → `.5`, com `.0` (Política v1.1.0 + re-aceite forçado) em paralelo livre. `comprovantes.3` (envio à LLM) não vai a produção antes de `.0` Done. Detalhes completos em [`docs/stories/EPIC-COMPROVANTES.md`](stories/EPIC-COMPROVANTES.md).

As frentes A/B/C abaixo (refinamento residual, features Phase 5+, débitos h8.*) continuam disponíveis como alternativas — mas a frente ativa é o EPIC-COMPROVANTES.

### O que ficou pronto na sessão anterior (2026-05-09 — 1 commit direto em main + CI fix)

| # | Commit | Conteúdo | SHA |
|---|--------|----------|-----|
| 1 | `feat(sidebar)` | Badge quadrado ivory na divisória direita do sidebar (centro vertical, flutuando sobre a borda); ícone PanelLeftClose ↔ PanelLeftOpen com affordance imediata; transição editorial 320ms ease-in-out-editorial; hover warm-shadow + scale-105; remove botão "Recolher" do rodapé. | `00bbe35` |
| 2 | `ci/secrets` | `SUPABASE_ACCESS_TOKEN` e `SUPABASE_PROJECT_REF` configurados via `gh secret set`; destrava job `Auth recovery template drift check`. | n/a (GitHub Actions secrets) |

### Smoke pendente (regra 4 RSC — só humano valida)

Abrir `usekeyra.com` autenticado em desktop (≥lg) e validar:
- Clicar o badge ~5x — animação 320ms editorial deve sentir suave e "elevada"
- Hover do badge — warm-shadow-md + scale-105 visíveis
- Tab no teclado — ring terracota canônico aparece
- F5 — estado collapsed/expanded persistido em localStorage

### O que continua aberto pra próxima sessão

#### Opção A — Telas faltantes (refinamento residual)

- `/configuracoes` — form-heavy, refinamento estético menor (baixa prioridade)
- Sub-rotas DRE-por-servico e DRE-por-profissional (já tinham padrão Sprint 6, podem ganhar SummaryCards igual `/dre` ganhou em #29)

#### Opção B — Features novas com schema novo (Phase 5+)

- **Pacote/Assinatura** no perfil de paciente (placeholder "Em breve" em #25) — schema novo de packages + sessions + billing
- **Pré-confirmação do cliente** na agenda (badge "Solicitações de aprovação") — schema novo + integração WhatsApp
- **Phase 5** Precificação inteligente (motor BOM + margem, pacotes, alertas de recompra, Stripe billing)
- **Phase 7** Integrações externas (OCR de extratos, Asaas Pix, WhatsApp Business, NFS-e)

#### Opção C — Resolver débitos técnicos remanescentes (Sprint 9 hardening)

- Stories h8.1-h8.8 em `docs/stories/h8.*.story.md`
- Inclui: typegen Supabase com privilégio correto, Sentry breadcrumb cooldown rate-alarm, cleanup automático `password_reset_attempts` >24h, etc.

### Como retomar contexto rápido na próxima sessão

1. Abra terminal no `~/keyra/`
2. Leia este `docs/STATE.md` (header já tem snapshot de 60s da sessão 2026-05-09)
3. Memory `project_keyra_state.md` é auto-loaded — confirma o que está em prod

---

## 6.bis. Snapshot anterior (sessão 2026-05-08 — encerrada)

🎉🎉🎉🎉🎉🎉🎉🎉🎉🎉 **9 PRs MERGEADOS EM MAIN + REDEPLOY VERCEL FRESH.** Aplicação completa do padrão Editorial Beauty Luxury KEYRA em todas as listas e dashboards. Sistema 100% atualizado em `usekeyra.com`.

### O que ficou pronto na sessão 2026-05-08 (9 PRs em prod)

| # | PR | Conteúdo | SHA |
|---|----|----------|-----|
| 1 | #22 | Dashboard editorial — 7 blocos (Hero + 3 KPIs hero + Schedule heatmap + DRE proporcional + Top Serviços + Major Expenses + Upgrade CTA) | `b270a4b` |
| 2 | #23 | Agenda Phase 1 — sidebar mini-cal + status segmented + search + ReceitaCardAnimated stagger | `a136f38` |
| 3 | #25 | Pacientes perfil completo — 2-col com KPIs animados + tabelas + seedDemoPatient | `7f223ac` |
| 4 | #27 | Catálogo redesign — 4 KPIs MoM + toolbar segmented + tabela unificada | `436b57a` |
| 5 | #28 | Comandas — 4 KPIs MoM + status segmented + lista refinada | `9e73f70` |
| 6 | #29 | DRE + Team — SummaryCards DRE + mini-stats Team + RolePill paleta | `3f3cf8a` |
| 7 | #30 | Insumos — 3 mini-stats + avatar Package + pills paleta KEYRA | `ac32d5c` |
| 8 | #31 | Hub /financeiro Fase 1 — 4 KPIs sparkline + monthly bars + comandas + top despesas | `ded90a0` |
| 9 | #32 | Financeiro Fases 2+3 — Expense Tracker animado + Daily Costing + Variance vs Meta | `ea453ff` |

### O que continua aberto pra próxima sessão

#### Opção A — Telas faltantes (refinamento residual)

- `/configuracoes` — form-heavy, refinamento estético menor (baixa prioridade)
- Sub-rotas DRE-por-servico e DRE-por-profissional (já tinham padrão Sprint 6, podem ganhar SummaryCards igual `/dre` ganhou em #29)

#### Opção B — Features novas com schema novo (Phase 5+)

- **Pacote/Assinatura** no perfil de paciente (placeholder "Em breve" em #25) — schema novo de packages + sessions + billing
- **Pré-confirmação do cliente** na agenda (badge "Solicitações de aprovação") — schema novo + integração WhatsApp
- **Phase 5** Precificação inteligente (motor BOM + margem, pacotes, alertas de recompra, Stripe billing)
- **Phase 7** Integrações externas (OCR de extratos, Asaas Pix, WhatsApp Business, NFS-e)

#### Opção C — Resolver débitos técnicos remanescentes (Sprint 9 hardening)

- Stories h8.1-h8.8 em `docs/stories/h8.*.story.md`
- Inclui: typegen Supabase com privilégio correto, Sentry breadcrumb cooldown rate-alarm, cleanup automático `password_reset_attempts` >24h, etc.

### Como retomar contexto rápido na próxima sessão

1. Abra terminal no `~/keyra/`
2. Leia este `docs/STATE.md` (header já tem snapshot de 60s da sessão 2026-05-08)
3. Memory `project_keyra_state.md` é auto-loaded — confirma o que está em prod
4. Sistema 100% Editorial Beauty Luxury aplicado: dashboard, agenda, pacientes, catálogo, comandas, DRE, team, insumos, hub financeiro, despesas analytics, metas variance.



### O que ficou pronto nesta sessão (4 PRs mergeadas em prod)

| # | PR | Conteúdo | SHA |
|---|----|----------|-----|
| 1 | #8 | Story `auth.5` inicial — recovery + cooldown + signOut global | `5d9e71d` |
| 2 | #9 | Story `auth.8` — BroadcastChannel cross-tab | `c54d443` |
| 3 | #10 | **🔴 P0 fix** — token_hash flow no callback handler (recovery estava 100% quebrado pra usuário real) + scripts de smoke E2E reais | `f5256ba` |
| 4 | #11 | Story `auth.9` — jornada afunilada + força senha + visual revamp AppShell | `3e3ab97` |

### Abertura disponível na próxima sessão (sua escolha)

#### Opção A — Rodar `auth.6` (Google OAuth) — fecha o EPIC em 100%

**Pré-condição mandatória sua:** setup manual no Google Cloud Console (criar OAuth 2.0 Client ID + consent screen + scopes email/profile/openid + Authorized redirect URI). Guia passo-a-passo completo em [`docs/setup/google-oauth-setup.md`](setup/google-oauth-setup.md). Tempo estimado: 10-15 min seu + 30-40 min meu (Supabase Mgmt API + código KEYRA + smokes).

#### Opção B — Pular pra Pós-MVP (recomendado, EPIC-AUTH-V2 não bloqueia mais nada)

3 frentes possíveis:

- **Phase 5 — Precificação inteligente** (motor BOM + margem, pacotes, alertas de recompra, Stripe billing). Squad: `squad-keyra-intelligence`. Workflow: `keyra-inteligencia-spec-sdc`
- **Phase 6 — Projeções & inteligência** (what-if, prontuário financeiro, rentabilidade por horário). Squad: `squad-keyra-intelligence`
- **Phase 7 — Integrações externas** (OCR de extratos, Asaas Pix, WhatsApp Business, NFS-e). Squad: `squad-keyra-integrations`. Workflow: `keyra-integracoes-spec-sdc`

Disparo: `@aiox-master *workflow {nome-do-workflow}` ou comando direto do squad.

#### Opção C — Resolver débitos técnicos remanescentes (Sprint 9 hardening)

- Stories h8.1-h8.8 em `docs/stories/h8.*.story.md` (descongela Sprint 8 hardening original — vira Sprint 9)
- Inclui: typegen Supabase com privilégio correto (script já é atômico, falta token); Sentry breadcrumb cooldown rate-alarm; cleanup automático `password_reset_attempts` >24h; etc.

### Como retomar contexto rápido na próxima sessão

1. Abra terminal no `~/keyra/`
2. Leia este `docs/STATE.md` (header já tem snapshot de 60s)
3. Memory `project_keyra_state.md` é auto-loaded — confirma o que está em prod
4. EPIC-AUTH-V2 com tabela 9/10 Done em `docs/stories/EPIC-AUTH-V2.md`
5. Mapa completo desta sessão de encerramento em `docs/sessions/2026-05-06-session-update-map.md`

### Próximas decisões — sua escolha

A Fase A do EPIC-AUTH-V2 está fechada. Posso abrir a Fase B (4 stories paralelas) imediatamente OU pausar para você fazer smoke real em produção primeiro.

**Opção 1 — Smoke em prod primeiro (recomendado)**:
- Você abre `usekeyra.com/login`, faz login com magic link como sempre, navega `/dashboard` `/agenda` `/comandas` `/financeiro` confirmando regressão zero
- Após confirmar OK, autoriza Fase B

**Opção 2 — Abrir Fase B em paralelo agora**:
- Eu abro 4 stories simultaneamente: `auth.1` (schema), `auth.2` (textos legais), `auth.4` (login senha), `auth.7` (JWT claim)
- Smoke em prod fica como gate quando elas chegarem em PR
- Risco: se algo do `auth.0` quebrou em prod sem você notar, descobrimos junto com mudanças novas — depuração fica mais difícil

**Recomendação:** Opção 1. Auth é zona crítica e o bug RSC de 2026-05-02 ensinou que "deploy READY ≠ produto funcional".

### O que já foi feito nesta sessão (commit pendente)

| Frente | Estado | Detalhe |
|--------|--------|---------|
| **Auditoria preventiva** | ✅ Persistida | `docs/audit/auth-v2-security-audit.md` — 22 riscos com fonte exata + 12 decisões de arquitetura travadas |
| **EPIC-AUTH-V2** | ✅ Aberto | `docs/stories/EPIC-AUTH-V2.md` — 10 stories, 44 pts, ordem de execução em 4 fases, gates Phase 3.5 mapeados |
| **Story auth.0** | 🟡 InProgress | `docs/stories/auth.0.story.md` — Draft → Ready → InProgress; 8 de 14 tasks ✅ |
| **Sentry scrubbing** | ✅ Implementado | `lib/observability/sentry-scrub.ts` (server+client) com `password`/`senha`/`cpf`/`phone`/`token`/`Authorization` redactados; **selftest verde** (`KEYRA_SENTRY_SCRUB_SELFTEST=1`) |
| **Helper Turnstile** | ✅ Implementado | `lib/security/verify-turnstile.ts` server-only com bypass dev controlado e fail-hard em prod |
| **Schema env Zod** | ✅ Estendido | `NEXT_PUBLIC_TURNSTILE_SITE_KEY` + `TURNSTILE_SECRET_KEY` (opcional até widget provisionado) |
| **`.env.local.example`** | ✅ Atualizado | Instruções passo-a-passo de Cloudflare Turnstile |
| **`sync-env.sh`** | ✅ Estendido | Lê `.keyra-secrets/turnstile-{site,secret}.key` com `read_or_empty` |
| **Script Management API** | ✅ Criado + dry-run validado | `scripts/configure-supabase-auth-prod.sh` idempotente, payload único PATCH com 7 configs universais + 2 de sessão tolerantes a plano |
| **ADR-022** | ✅ Publicado | `ARCHITECTURE.md` §11.2 — referencia auditoria, lista trade-offs, riscos cobertos, pendências jurídicas postergadas |
| **STATE.md** | ✅ Sincronizado | Header + §1 + §6 + §8 atualizados nesta sessão |
| **Quality gates** | ✅ Todos verdes | `pnpm typecheck` ✅ · `pnpm lint --max-warnings 0` ✅ · `./scripts/check-rsc-boundaries.sh` ✅ PASS |

### Risco a saber

A configuração endurecida do Supabase (`mailer_autoconfirm=false`, etc.) **NÃO quebra usuários existentes** — magic link conta como confirmação automática de email, e ainda não há fluxo de senha implementado em prod. Aplicação é segura. Se quiser confirmar antes, posso explicar o porquê.

---

### Estado anterior (referência — ainda válido para o produto MVP)

🟢 **Sessão HOTFIX RSC encerrada com reforço estrutural anti-regressão definitivo em main.** Toda rota autenticada carrega em produção real. Doravante, qualquer regressão do mesmo tipo é **bloqueada no CI** antes de chegar em produção.

| Frente | Estado | Detalhe |
|--------|--------|---------|
| **(app)/dashboard** | ✅ Carrega completo | 4 KPIs + AlertasCard + AgendaHojeCard + IndicadoresCard + MetaCard. Todas as queries retornam OK |
| **(app)/clientes (ex-pacientes)** | ✅ Carrega | Renomeado em sidebar/bottomnav/h1/copy. URL mantida `/pacientes` |
| **(app)/serviços (ex-comandas)** | ✅ Carrega | Renomeado em sidebar. URL mantida `/comandas` |
| **(app)/catálogo (ex-serviços)** | ✅ Carrega | Renomeado em sidebar/h1. URL mantida `/servicos` |
| **(app)/financeiro** | ✅ Carrega | Inalterado |
| **(app)/estoque** | ✅ Carrega | Inalterado |
| **Sidebar** | ✅ Sticky + colapsável | `sticky top-0 h-screen` + toggle persistido em localStorage |
| **KPI proporção** | ✅ Tokens reduzidos | `kpi-hero` 56→36px, `kpi` 40→28px |
| **Script audit RSC** | ✅ Ativo | `scripts/check-rsc-boundaries.sh` resolve imports reais, detecta R1 (LucideIcon prop), R3 (useSyncExternalStore), R4 (Server→Client com Lucide). Zero falsos positivos |
| **CI gate `rsc-audit`** | ✅ Em workflow | `.github/workflows/rls-tests.yml` job `rsc-audit` roda em todo push/PR. PR não merge se falhar |
| **CLAUDE.md + story-lifecycle.md** | ✅ Atualizados | `docs/dev/rsc-boundary-rules.md` é leitura obrigatória. Phase 2.5 G5 instaurado |

**Próximo passo crítico (próxima sessão):** validação ponta-a-ponta com a persona Camila simulada por Luiz — **Jornada 1 (Onboarding)** e **Jornada 2 (Dia típico)** conforme `docs/ux/user-journeys.md`. Criar 1 cliente real, 1 serviço no catálogo, 1 agendamento, concluir, registrar pagamento, ver KPIs subirem no Dashboard. Esse é o teste que prova FUNCIONAMENTO, não só carregamento.

**Após J1+J2 validadas:**

1. **Pós-MVP Phases 5–7** — Phase 5 (Precificação inteligente, pacotes, alertas de recompra, Stripe), Phase 6 (Projeções, prontuário financeiro, what-if), Phase 7 (OCR de extratos, Asaas Pix, WhatsApp, NFS-e).
2. **Story de motion volta segura** — reintroduzir `ScrollFadeRise` no Dashboard com pattern correto (sem `display: contents`, validada via Sentry em prod e G5 audit verde).

### Caminho para Sprint 6 (camada editorial)

A Story 5.7 fechou todos os furos de cobertura levantados pelo `baziotti` (audit `docs/ux/audit-2026-05-01-sprint5-review.md`). A Sprint 6 entra em base 100% consistente:

| Story | Foco | Estimativa |
|-------|------|------------|
| 6.1 | **Inter Variable + escala expandida** (weights 200–700, ritmo editorial Gestek) | M |
| 6.2 | **Motion / framer-motion** (transições Linear-style entre rotas, micro-interações) | L |
| 6.3 | **Alert hierarchy 3+5+5** (3 níveis × 5 estados × 5 padrões dismiss) — refactor do `<AlertCard>` | M |
| 6.4 | **DRE refactor visual** (densidade, hierarquia, tipografia editorial) | M |
| 6.5 | **Sidebar 7 itens + FAB contextual** (label/destino por rota) + dashboard "estado dia 1" com welcome card | M |

```text
@sm *draft 6.1   # Inter Variable (planejada em docs/ux/PLANO-SPRINT-5-6.md)
@po *validate-story-draft 6.1
@dev *develop-story 6.1
@qa *qa-gate 6.1
@devops *push
# repete para 6.2 → 6.3 → 6.4 → 6.5
```

**Recomendação tática:** começar pela **6.1 (Tipografia)** porque ela é a base visual da camada editorial — todas as stories 6.2-6.5 herdam o sistema tipográfico. Inverter a ordem força refactor.

### Smoke test pós-deploy da Sprint 3 (executar antes da Sprint 4)

Em `https://usekeyra.com`:

1. **Ciclo completo**: `/agenda` cria agendamento → conclui → `/comandas` mostra comanda aberta → finalizar → registrar pagamento (Pix R$ X) → comanda vira paga.
2. **`/comandas/[id]`**: card "Pagamentos" lista o pagamento; card "Consumo de insumos" aparece (se serviço tem BOM).
3. **`/financeiro/transacoes`**: a transação aparece automaticamente com origin="command_payment".
4. **`/financeiro/receitas`**: filtra por período → vê receita do profissional.
5. **`/financeiro/despesas/nova`**: cria despesa manual → aparece em transações como debit.
6. **`/financeiro/custos-fixos`**: marcando "fixo" no form, aparece aqui no mês corrente.
7. **`/financeiro/fluxo-caixa`**: saldo atual, tabela diária, projeção via `v_receitas_previstas`.
8. **`/estoque/movimentacoes?command={id}`**: filtro por comanda mostra apenas o consumo daquele atendimento.

### Smoke test pós-Story 2.6 (executar antes da próxima sessão de @dev)

Em `https://usekeyra.com/agenda`, com um agendamento `scheduled`:

1. Click no evento → sheet com badge azul "Agendado" + 3 botões.
2. **Concluir atendimento** → AlertDialog → confirmar → toast "Atendimento concluído. Comanda gerada automaticamente."
3. No Supabase Studio (SQL Editor):
   ```sql
   select id, appointment_id, status, subtotal
     from public.commands
    where appointment_id = '{id-do-agendamento}'
    order by created_at desc;
   ```
   Deve retornar 1 linha em `status='open'` (concern AC2.2 confirmado empiricamente).
4. Repetir para `cancelled` (combobox + "Outro" → digitar texto → confirmar) e `no_show`. Verificar `audit_log` carrega as 3 transições:
   ```sql
   select action, after->>'status' as new_status, created_at
     from public.audit_log
    where resource_type = 'appointments'
      and resource_id = '{id}'
    order by created_at;
   ```
5. Click em evento `cancelled` → sheet mostra badge cinza + aviso "somente leitura"; nenhum botão de transição.
6. Click em evento `done` → sheet mostra badge verde + único botão "Cancelar" (correção; estornar comanda é Phase 3+).

### Pendência operacional única ainda em aberto

- **`apps/web/.vercel/`** foi linkado em sessão anterior e está gitignored (`.vercel/`) — a próxima sessão pode reutilizar. Caso outra máquina precise, rodar `vercel link --project keyra --scope luiz-henrique-sealdigital --token=$VERCEL_TOKEN --yes`.

---

## 7. Referências Canônicas

Quando precisar de detalhes, abrir na ordem:

1. **Visão geral do plano** → [`docs/stories/EPIC-0-KEYRA-IMPLEMENTATION.md`](stories/EPIC-0-KEYRA-IMPLEMENTATION.md)
2. **Mapa feature × tela × tabela × ADR × story** → [`docs/IMPLEMENTATION-MAP.md`](IMPLEMENTATION-MAP.md)
3. **Requisitos funcionais (66 FR + 27 NFR + 27 CON)** → [`docs/prd/PRD-KEYRA.md`](prd/PRD-KEYRA.md)
4. **Decisões técnicas (20 ADRs)** → [`docs/architecture/ARCHITECTURE.md`](architecture/ARCHITECTURE.md)
5. **Schema do banco** → [`docs/architecture/SCHEMA.md`](architecture/SCHEMA.md) + [`supabase/migrations/`](../supabase/migrations/)
6. **Estado da infraestrutura** → [`docs/INFRA-STATUS.md`](INFRA-STATUS.md)
7. **Wireframes** → [`docs/ux/wireframes/`](ux/wireframes/)
8. **Pesquisa competitiva** → [`docs/research/`](research/)

---

## 8. Histórico deste Snapshot

| Data | Mudança |
|------|---------|
| 2026-05-19 (governança de design system) | **Regra-mãe de design system criada, aplicada e deployada.** PR #35 mergeado: `.claude/rules/design-system.md` (8 princípios inegociáveis com bloco editável — tokens, paleta quente, Lei da Proporção Espacial), `scripts/check-design-system.sh` + job CI `design-system-audit`, registro em `.claude/CLAUDE.md`. **Correções:** `StatusBadge.tsx` (4 badges migrados de `stone-*`/`rose-*` → tokens KEYRA) e `apps/web/public/landing.html` (8 `<br>` manuais removidos eliminando palavras órfãs; `.cost-card`/`.app-msg` com conteúdo centralizado; `.plan-name` com piso de 2 linhas). Redeploy `653f8fc` para invalidar edge cache da Vercel (`x-vercel-cache: HIT`, age ~12h). Produção verificada via headless Chrome. Tarefa direta de `@aiox-master` (não-story). Memória `feedback_design_system_rule.md` registrada. **Tech-debt aberto:** 28 HEX literais em `.tsx` (gráficos SVG do `/financeiro`, e-mail, logo Google) — WARN não-bloqueante. Smoke visual humano da landing pendente. |
| 2026-05-18 (planejamento EPIC-COMPROVANTES) | **Phase 7.0 — Comprovantes Inteligentes — planejada, revisada, validada e mergeada em `main`.** PR #34 (`1d7bac8`, squash, 14 arquivos, +3.144 linhas). **A feature:** a usuária anexa um comprovante em qualquer formato (foto, print, PDF, DOCX, ODT, TXT, MD, HTML, EPUB), uma LLM (`openai/gpt-4o-mini` via Vercel AI Gateway, `disallowPromptTraining`) lê e extrai os dados financeiros (valor, data, direção, descrição, contraparte), a usuária revisa e o sistema registra como `transactions` — receita ou despesa (escopo entrada+saída). **Investigação inicial:** descoberto que um EPIC-COMPROVANTES havia sido projetado numa sessão anterior mas existia só na memória do agente — nenhum artefato commitado; reconstrução do zero. **Artefatos criados:** ADR-023 em `ARCHITECTURE.md` §11.3 (provider lock + arquitetura + matriz multi-formato com camada de normalização), `EPIC-COMPROVANTES-SPEC.md` (spec arquitetural — pipeline 6 estágios, schema `receipts`, Zod, segurança, TD-CMP), `EPIC-COMPROVANTES.md` (epic), `privacy-v1.1.0-DRAFT.md` (Política de Privacidade com seção de uso de IA + subprocessador OpenAI + re-aceite forçado), `COMPLIANCE-AUDIT-EPIC-COMPROVANTES.md` (auditoria LGPD preventiva — 11 achados: 1 CRÍTICO, 5 ALTO, 4 MÉDIO, 1 BAIXO; veredito CONCERNS endereçável), 7 stories `comprovantes.0`–`.5`. **Revisão técnica:** `@architect` e `@document-processor` revisaram a spec → veredito NEEDS_REVISION, 3 bloqueadores factuais corrigidos: (1) CHECK `transactions.source_type` não aceita `'document'` → micro-migration aditiva em `.1`; (2) `gpt-4o-mini` não lê PDF como file part nativo → rasterização para imagem sempre, com spike de viabilidade em serverless; (3) RLS de `storage.objects` sem precedente no KEYRA → spike obrigatório no início de `.1`. Story `comprovantes.4` (XL, 13 pts) dividida em `.4a` (UI lista/upload) + `.4b` (revisão/registro) para isolar o gate RSC do gate financeiro. Spec promovida a rev. 2; pareceres salvos em `docs/architecture/reviews/`. **Validação:** `@po` validou as 7 stories pelo checklist de 10 pontos — GO 10/10 cada, `Draft → Ready`. **Auditoria de consolidação (`@aiox-master`/Orion):** leitura das 7 stories ponta a ponta + cruzamento com a auditoria de compliance e os pareceres → achado CMP-M2 (renderização segura do documento na revisão) ficara órfão na divisão de `.4`; corrigido em `comprovantes.2` AC5.6 (persistir artefato normalizado renderizável) + `comprovantes.4b` AC1.2/AC2.2 (revisão renderiza só o normalizado, nunca o original cru). **`@devops`:** branch `docs/epic-comprovantes-planejamento`, commit `fdd2ff9`, push, PR #34 aberto, CI verde (`MERGEABLE`/`CLEAN`), merge squash → `1d7bac8` em `main`, branch deletada. **Nada de código de aplicação — só planejamento.** 2 spikes obrigatórios na implementação. `comprovantes.3` bloqueada para prod até `comprovantes.0` Done (Política v1.1.0 + re-aceite forçado ativo). **Próximo:** `@dev *develop-story comprovantes.1` (fundação de schema), com `.0` paralelizável. |
| 2026-05-09 (refinamento sidebar + CI hardening) | **Refinamento UX entregue direto em main + CI fix.** Commit `00bbe35` (`feat(sidebar): badge flutuante editorial + animação warm-shadow`) substitui o botão "← Recolher" do rodapé do sidebar por um badge quadrado ivory ancorado no centro vertical da divisória direita do sidebar (flutua sobre a borda entre sidebar e conteúdo); ícone alterna `PanelLeftClose` ↔ `PanelLeftOpen` para affordance imediata da ação que vai ocorrer; warm-shadow-md + scale-105 no hover; ring terracota canônico em focus-visible para acessibilidade por teclado; transição editorial em `transition-[width,box-shadow] duration-base ease-in-out-editorial` (320ms cubic-bezier 0.65,0,0.35,1) sustenta sensação de elevação durante o gesto. **Decisão arquitetural:** mudança visual atômica de baixo risco (1 arquivo, 43+/23-) entregue direto em main sem PR — não há comportamento novo, só refinamento de affordance no toggle existente. **CI hardening (paralelo):** secrets `SUPABASE_ACCESS_TOKEN` e `SUPABASE_PROJECT_REF` configurados no GitHub Actions via `gh secret set` lendo de `.keyra-secrets/supabase.token` e `.keyra-secrets/supabase-project-ref.txt`; destrava o job `Auth recovery template drift check` que vinha falhando desde a configuração do workflow (mensagem `❌ Faltando SUPABASE_ACCESS_TOKEN e/ou SUPABASE_PROJECT_REF`). Não bloqueava deploy Vercel (auto-deploy é independente do GHA), mas mantinha o badge de CI vermelho e tirava sinal de outras falhas. **Validação técnica:** typecheck verde, ESLint zero warnings, build verde (42 rotas), RSC Boundary Audit PASS, Vercel deploy `https://keyra-edmpugpwk-luiz-henrique-sealdigital.vercel.app` com state=success em 2026-05-09T21:37:36Z, produção `usekeyra.com` HTTP 200. **Pendente NÃO automatizável:** smoke real do gesto de toggle no desktop (≥lg) confirmando feel editorial em transição 320ms (regra 4 RSC). |
| 2026-05-08 (sweep editorial completo + financeiro 3 fases) | **9 PRs mergeados em main + redeploy fresh disparado.** Aplicação completa do padrão Editorial Beauty Luxury KEYRA em todas as listas e dashboards do MVP, fechando o ciclo de refinamento iniciado pelo Epic BRAND-INTEGRATION. PRs entregues: **#22 Dashboard editorial** (`b270a4b`) com 7 blocos (Hero saudação + 3 KPIs hero com pill buttons + Schedule heatmap mensal + DRE proporcional segmentado + Top Serviços + Major Expenses + Upgrade CTA cocoa-900); **#23 Agenda Phase 1** (`a136f38`) layout 2-col com sidebar mini-cal + counts + próximo agendamento, status tabs em segmented control, search textual, ReceitaCardAnimated com stagger 80ms (resolveu overlap sticky com header Fraunces); **#25 Pacientes perfil completo** (`7f223ac`) layout 2-col com PatientHeaderCard avatar gradient + Overview + PatientPackageCard placeholder pacote/assinatura + 4 KPIs animados (Total gasto/Pendente/Atendimentos/Frequência) + tabelas atendimentos e pagamentos + Server Action `seedDemoPatient` cria "Maria Silva (Demo)" com 5 atendimentos + 3 comandas pagas + 3 transações; **#27 Catálogo redesign** (`436b57a`) com 4 KPIs MoM (Total/Ativos/Categorias/Sem categoria) + toolbar segmented + tabela unificada com pills coloridos por categoria; **#28 Comandas** (`9e73f70`) com 4 KPIs MoM + status tabs em segmented + lista refinada com avatar Receipt gradient; **#29 DRE + Team** (`3f3cf8a`) DRE com 3 SummaryCards (Receita líquida terracota + Lucro success/destructive + Custos cocoa) e tabela com hierarquia subtotal/final em Fraunces 24px com cor por sinal; Team com 3 mini-stats inline + avatar gradient + RolePill paleta KEYRA (owner cocoa, admin terracota, professional amber, viewer mocha); **#30 Insumos** (`ac32d5c`) com 3 mini-stats + avatar Package gradient mocha→bronze + pills "Recompra" amber e "Inativo" mocha; **#31 Hub /financeiro Fase 1** (`ded90a0`) substituindo redirect por landing com 4 KPIs SVG sparkline inline (Receita/Despesas/Lucro/Saldo ano) + variação MoM colorida + stacked bars Jan-Dez (Receita terracota + Despesas cocoa) com motion stagger + comandas em aberto com priority flag (cliente recorrente = 3+ paid em 12m) + top 5 categorias com mini progress bars; **#32 Financeiro Fases 2+3** (`ea453ff`) ExpenseTrackerAnimated com SVG path animado área cocoa/terracota + linha de média tracejada + highlight de hoje em círculo terracota, DailyCostingChart com barras da semana e hoje destacado em terracota com tooltip flutuante, VarianceTable em /metas com Realizado vs Meta mês a mês e status pills (▲ Atingiu success-leaf / ◐ Parcial amber / ▼ Abaixo rust-800 / Sem meta mocha). **Server Actions novas (12):** `getCommandsStats`, `getCatalogStats`, `getPatientProfile`/`Encounters`/`Payments`, `seedDemoPatient`, `getTopServicesByRevenue`, `getExpensesByCategoryThisMonth`, `getMonthScheduleHeatmap`, `getMonthCounts`, `getFinanceOverview`, `getOutstandingCommands`, `getTopExpensesByCategory`, `getExpenseAnalytics`, `getVarianceMonthly`. **Princípio UX atualizado** (feedback idealizadora 2026-05-07): regra "zero gráficos" rebaixada de inegociável para preferência hierárquica — números absolutos continuam protagonistas, mas tabelas e gráficos proporcionais simples (barras horizontais segmentadas, mini-bars de comparação, sparklines, heatmaps por densidade) permitidos como reforço quando ajudam a ler o todo. Memory `feedback_ux_principles.md` atualizada. **Adaptações vs referências (KlarityOS Img 15/18/21 + Kodo Img 22 + Studio 813 Img 7 + Prodless Img 6) com justificativa explícita:** WHT/VAT separados descartados (tributação asiática, KEYRA usa Simples Nacional/DAS único); plano de contas Account No / Parent:Child descartado (ERP corporativo, KEYRA é gestão de clínica de estética); A/P Aging descartado (sem fornecedores B2B com prazo); workflow "For Preparer" descartado (KEYRA é admin único, sem hierarquia de approval); Client portal vs Super admin tools descartado (não é multi-tenant ERP); Tax ID / Invoice Counter / PO descartados (comandas têm UUID auto-gerado); Reconciliação bancária real adiada para Phase 7 (OCR comprovantes); paleta roxa Kodo → cocoa/terracota canônica KEYRA preservada. **Redeploy fresh disparado** via commit empty `1ce33cd` (chore/ops trigger Vercel) + cada merge dos 9 PRs disparou auto-deploy independente. **Validação produção:** 8 rotas autenticadas respondendo HTTP 307 → /login (auth gate ativo) + /comecar HTTP 200 (sales page pública). Quality gates em todos os 9 PRs: typecheck verde, ESLint zero warnings, RSC Boundary Audit PASS. **Lições operacionais salvas:** stash com pathspec deve cobrir TODOS tracked modificados não relacionados (PR #23 perdeu 3 arquivos do logo brand work por pathspec incompleto); aplicar idempotência em Server Actions de seed (`seedDemoPatient` busca cliente existente antes de criar). |
| 2026-05-03 (sessão `auth.0` Done em produção) | **PR #3 aberta com 14 conformidades verificadas em pre-flight** (git status, divergência vs main, diff stat, sem secrets vazados, gh auth, typecheck, lint, RSC audit, scrub selftest, Vercel preview todos READY, 3 CI checks PASS, mergeable status). PR body com 9 seções estruturadas (sumário, entregue, riscos, gates, descoberta operacional, test plan, próximas stories). **CI checks PASS:** RSC Boundary Audit em 18s + Cross-tenant isolation (RLS suite) em 39s + Vercel deploy SUCCESS. Idealizadora autorizou merge direto via Opção B. **`@devops` (Gage) executou** `gh pr merge 3 --squash --delete-branch` — 3 commits squashed em `dbc753e` em main, branch `feat/auth-v2` deletada local + remote. **Vercel prod deploy READY em ~75s** — `usekeyra.com` agora roda com Sentry PII scrubbing ativo + Supabase Auth endurecido (mailer_autoconfirm=false, password_min_length=10 + complexity, security_update_password_require_reauthentication=true, mailer_otp_exp=1800, rate_limit_email_sent=30, refresh_token_rotation_enabled=true) + Turnstile envs disponíveis nos 3 targets prontas para `auth.3-6`. Story `auth.0` Status Ready for Review → **Done** com 14/14 tasks ✅, gate `@compliance-br` APPROVE + 2 CONCERNS propagados como dependências cruzadas em `auth.2`. **Foundation de segurança do EPIC-AUTH-V2 entregue ponta-a-ponta — 4 críticos (R1, R2, R3, R16) + 4 altos (R11, R12, R14, R20) + 1 parcial (R22) mitigados em prod.** Lições operacionais novas salvas em memory: `feedback_supabase_patch_silent_drop.md` e `reference_turnstile_widget.md`. **Próximo:** smoke em prod pela idealizadora, depois Fase B do EPIC (auth.1 + auth.2 + auth.4 + auth.7 paralelos). |
| 2026-05-03 (sessão fechamento `auth.0` Ready for Review) | **Foundation de segurança do EPIC-AUTH-V2 entregue.** Idealizadora criou widget Turnstile (`keyra`, Managed, hostnames `usekeyra.com`+`localhost`), entregou site_key+secret_key. Secret validada via API Cloudflare (`POST /siteverify` com token sintético retornou `invalid-input-response` — prova que a secret é reconhecida; `invalid-input-secret` indicaria errada). Chaves salvas em `.keyra-secrets/turnstile-{site,secret}.key` chmod 600; `sync-env.sh` rodado; Vercel envs provisionadas via REST API v10 nos 3 targets (Production+Preview+Development) e validadas via GET. Idealizadora autorizou execução do script de config Supabase em prod ("Já tá autorizado. Go, rode, vai, execute."). **Script rodado e bem-sucedido — 7/7 configs universais aplicadas e validadas via GET**: `mailer_autoconfirm=false`, `password_min_length=10`, `password_required_characters` lower+upper+digits, `security_update_password_require_reauthentication=true`, `mailer_otp_exp=1800`, `rate_limit_email_sent=30`, `refresh_token_rotation_enabled=true`. **Descoberta operacional crítica durante a execução:** primeira passada do script enviou `secure_password_change`, `session_timebox`, `session_inactivity_timeout` no PATCH e recebeu HTTP 200 — mas o GET subsequente revelou que os 3 campos NÃO foram aplicados. Supabase Management API silenciosamente descarta campos com naming errado e ainda retorna 200. Os naming canônicos reais (descobertos inspecionando o GET response) são `security_update_password_require_reauthentication`, `sessions_timebox` (plural), `sessions_inactivity_timeout` (plural). Re-patch isolado com `security_update_password_require_reauthentication=true` aplicou (HTTP 200 confirmado via GET). `sessions_timebox`+`sessions_inactivity_timeout` retornaram HTTP 402: "User sessions can only be configured on Pro Plans" — R22 da auditoria fica parcialmente mitigado (deferido junto com Vercel Pro decision). **Script `configure-supabase-auth-prod.sh` foi reescrito** com naming canônico correto + validação GET integrada como defesa contra silent-drop + tratamento explícito de Pro-only (HTTP 402 → SKIPPED-not-failure). **Memory `feedback_supabase_patch_silent_drop.md` registrada** estendendo regra anterior "validar credenciais via API antes de provisionar" para também cobrir "validar configuração aplicada via GET após PATCH". Story `auth.0` foi atualizada de InProgress → Ready for Review com 14/14 tasks ✅. **Pendente:** commit final consolidado + push + gate `@compliance-br` review + PR merge em main. Após Done, libera Fase B (auth.3 cadastro + auth.4 login senha + auth.7 JWT claim em paralelo). |
| 2026-05-03 (sessão de mapeamento UX + abertura EPIC-AUTH-V2) | **Sessão estratégica conduzida pela idealizadora identificou login "muito feio" + único método (magic link) inadequado para uso comercial + ausência total de cadastro estruturado + ausência de Google OAuth + bug de 2 abas no fluxo magic link.** Auditoria preventiva de segurança gerada antes de qualquer linha de código (a pedido explícito da idealizadora: "garanta que dê certo, baseado em dados, sem adivinhação"). Resultado: 22 riscos identificados com fonte exata (arquivo:linha ou ausência confirmada via grep), agrupados em 7 críticos / 9 altos / 4 médios / 2 baixos. 12 decisões de arquitetura travadas (senha 10 chars + complexidade, Cloudflare Turnstile, email confirmation obrigatório, phone encrypted+last_four, JWT só com `org_id`+`full_name`, CNPJ unique parcial, RPC `signup_create_account_atomic` com compensating delete, trigger `on_auth_user_created`, `legal_documents`+`user_consent_records` versionados, signOut global pós-troca senha, OTP 30min, hook `before_user_created` bloqueando descartáveis). Plano final: **EPIC-AUTH-V2 com 10 stories** (`auth.0` config, `auth.1` schema, `auth.2` termos, `auth.3` cadastro, `auth.4` login, `auth.5` esqueci senha, `auth.6` Google OAuth, `auth.7` JWT claim, `auth.8` 2 abas fix, `auth.9` visual revamp; total 44 pts). Pendências jurídicas postergadas com placeholders (KEYRA ainda não tem CNPJ — retenção 30d decidida). **Branch `feat/auth-v2` criada de main**. Story `auth.0` (foundation de segurança — pré-requisito) draftada (@sm), validada 10/10 (@po), em InProgress (@dev): código entregue — Sentry scrubbing server+client com selftest verde (R16), helper Turnstile `verify-turnstile.ts` server-only com bypass dev controlado e fail-hard em prod (R3), env schema Zod estendido com `NEXT_PUBLIC_TURNSTILE_SITE_KEY`+`TURNSTILE_SECRET_KEY`, `.env.local.example` com instruções de provisionamento Cloudflare, `sync-env.sh` lendo `.keyra-secrets/turnstile-{site,secret}.key`, **script idempotente `configure-supabase-auth-prod.sh`** com dry-run validado (mailer_autoconfirm=false, password_min_length=10, password_required_characters lower+upper+digits, secure_password_change=true, mailer_otp_exp=1800, rate_limit_email_sent=30, refresh_token_rotation_enabled=true, session_timebox=168h+inactivity=8h tolerantes a plano), **ADR-022 publicado em ARCHITECTURE.md §11.2** referenciando auditoria preventiva. **Typecheck ✅ verde + Lint ✅ zero warnings + RSC audit ✅ PASS.** Pausado aguardando idealizadora: (1) criar conta Cloudflare Turnstile e entregar site_key+secret_key; (2) autorizar execução do script de config Supabase em prod (mudança em sistema externo — não executei sem aprovação explícita). Antiga Sprint 8 hardening (h8.1-h8.8) congelada — vira Sprint 9 quando Auth V2 fechar. Documento da auditoria persistido em `docs/audit/auth-v2-security-audit.md` (744 linhas com fontes citadas — checklist de Done do epic). |
| 2026-05-02 (sessão HOTFIX RSC + reforço estrutural anti-regressão) | **🔴→🟢 Idealizadora abriu produção autenticada e descobriu que toda rota `(app)/*` exceto Agenda quebrava com digest `3213099672` apesar de Sprint 7 ter fechado "Done" 4 dias antes.** Causa raiz: violação sistemática de fronteira RSC (forwardRef como prop Server↔Client + Client importando Server + `useSyncExternalStore` em Next 16). **Sequência de fixes em produção (5 commits):** `d39c84d` (EmptyState Server), `7f2a7ef` (AlertasList InlineEmptyState), `4853c53` (useDismissedAlerts sem useSyncExternalStore), `742b3f2` (cleanup + 4 regras documentadas em `docs/dev/rsc-boundary-rules.md`), `f5abbdb` (renames Pacientes→Clientes / Comandas→Serviços / Serviços (catálogo)→Catálogo + Sidebar `sticky top-0 h-screen` + toggle minimizar com localStorage + KPI tokens reduzidos `kpi-hero` 56→36px e `kpi` 40→28px + script `scripts/check-rsc-boundaries.sh` + CI job `rsc-audit`). **Reforço estrutural definitivo (commit final):** script de audit smart resolve imports reais e detecta Server passando Lucide para Client sem falsos positivos Server→Server, `.claude/CLAUDE.md` torna leitura de `docs/dev/rsc-boundary-rules.md` obrigatória para qualquer agente AIOX que toque UI/Client/Server boundary, `.claude/rules/story-lifecycle.md` Phase 2.5 G5 (RSC Boundary Audit) instaurado — story Done sem rodar audit volta com NO-GO retroativo. **AlertCard prop `icon?: LucideIcon` removida** (era vulnerabilidade R1 mesmo sem chamadores externos hoje — script detectou). **4 regras inegociáveis registradas:** (1) nunca passar forwardRef como prop através de Server↔Client; (2) Client não importa Server; (3) `useSyncExternalStore` proibido — usar `useState + useEffect + queueMicrotask`; (4) build verde NÃO é funcional, smoke real da idealizadora em mobile autenticado é critério de Done não negociável. **Lição operacional:** TypeScript valida tipos, ESLint valida convenções, Vercel build empacota — nenhum prova que produto roda com sessão real. Sprints 5/6/7 fecharam Done sem smoke real, o que originou esta cascata de bugs. Daqui pra frente, smoke ponta-a-ponta antes de declarar Done, sem exceção. |
| 2026-04-20 (sessão A) | Documento criado. Phase 0 fechada. Phase 1 em ~60%. Story 1.2 endurecida com patches de RLS. Próxima: Story 1.3. |
| 2026-04-20 (sessão B) | Stories 1.3 e 1.4 implementadas (tech debt zerado, ADR-021 Resend, convites + roles, suíte RLS + CI + badge). Phase 1 = 100% pendente 7 passos de aceite. |
| 2026-04-20 (sessão C) | Migration 022 aplicada no remoto, typegen feito, testes RLS passam local. Sprint 2 iniciado: Stories 2.1 (pacientes) e 2.2 (serviços + categorias) implementadas. Drafts 2.3–2.7 criados. |
| 2026-04-30 | **Aceite formal da Phase 1.** Verificação cruzada: 22/22 migrations no remoto, typecheck/lint zerados, sem `as never` em `team/actions.ts`, PR #1 mergeado em `main`. Resend ativado: `RESEND_API_KEY` + `EMAIL_FROM=KEYRA <no-reply@usekeyra.com>` em `.env.local` (raiz e `apps/web/`); domínio `usekeyra.com` no Resend em **Partially Verified** (SPF+DKIM ok, DMARC pendente). `scripts/sync-env.sh` estendido para ler `resend-api.key` e `email-from.txt`. Default em `env.ts` migrado de `keyra.app` para `usekeyra.com`. Pendências operacionais: (1) smoke test manual no dev, (2) provisionamento das vars Resend no Vercel, (3) DMARC final no Hostinger DNS. |
| 2026-04-30 — sessão @devops (smoke + DNS + envs) | **Smoke test em dev passou:** convite enviado para `luizxhenriquepro@gmail.com` chegou na caixa principal do Gmail com remetente `KEYRA <no-reply@usekeyra.com>`, template renderizado, link `/invites/{token}` válido — **Phase 1 validada de ponta a ponta**. Sessão @devops fechou: nameservers de `usekeyra.com` migrados Hostinger → Vercel; 5 registros DNS do Resend confirmados (`send` SPF, `send` MX, `resend._domainkey`, `_dmarc`); domínio agora **Verified** no Resend; `RESEND_API_KEY` (nova chave `re_9UB...`) + `EMAIL_FROM` provisionados em Production/Preview/Development do Vercel (REST API para Preview por causa de bug do CLI 50.38.x); commits `14696eb` (acceito Phase 1) e `6fd47b7` (polish: copy real na landing + magic link via Resend SMTP no Supabase) em `main`. SMTP customizado ativo: magic link de login passa a sair como `KEYRA <no-reply@usekeyra.com>` em vez do default `Supabase Auth`. |
| 2026-04-30 — sessão de canonização do domínio | **`usekeyra.com` consolidado como canônico em todo lugar.** Helper `getAbsoluteUrl()` (apps/web/src/lib/url.ts) reordenado: 1ª preferência `NEXT_PUBLIC_SITE_URL`, 2ª headers (preview/dev), 3ª fallback `https://usekeyra.com`. Vercel: `NEXT_PUBLIC_SITE_URL=https://usekeyra.com` provisionado em Production+Preview (Development fica vazio para `pnpm dev` continuar usando localhost). Supabase Auth: `site_url` migrado de `https://usekeyra.vercel.app` para `https://usekeyra.com`; `uri_allow_list` atualizado removendo `keyra.app` (domínio antigo descontinuado) e adicionando `usekeyra.com/auth/callback`. Convite vigente reenviado pelo Resend com link `usekeyra.com`. Commits `0a99da8` (docs) e `7660754` (helper). |
| 2026-04-30 — fix do `?next=` no magic link | **Bug crítico corrigido — fluxo de convite ponta-a-ponta.** Diagnóstico: ao clicar "Aceitar convite" no email, o convidado caía em `/onboarding/nova-organizacao` em vez de aceitar — o `?next=` setado por `/invites/[token]` não sobrevivia ao magic link. Causa: 5 elos quebrados (page do login, form, action, callback, proxy). Correção: helper `apps/web/src/lib/auth/safe-next.ts` (`isSafeNextPath` valida contra open redirect — recusa `//`, `://`, paths não relativos) + propagação de `next` em toda a cadeia. `signInWithOtpAction` agora monta `emailRedirectTo=${origin}/auth/callback?next=<encoded>`; `/auth/callback` lê e valida `?next=` antes de cair no roteamento por membership; `proxy.ts` respeita `next` quando user logado bate em `/login?next=`. `uri_allow_list` do Supabase expandido com wildcard `**` em todos os origins do callback para aceitar query strings. Commit `719783e` (6 arquivos, +88 -13). Smoke test em prod confirmou aceite do convite e entrada no dashboard da clínica. |
| 2026-04-30 — Sprint 2 destrancada (@po + Story 2.4) | **5 stories validadas pelo `@po`** (drafts 2.3-2.7 → todas Ready). Aplicados fixes mínimos em 2.5/2.6/2.7 (DoD ausente, Dependencies, UX gaps); enriquecida 2.4 com Dev Notes (timezone, locale, role, cap eventos). **Story 2.4 (Agenda) implementada e validada em produção**: 5 arquivos novos em `app/(app)/agenda/`, 2 componentes shadcn (`sheet`, `select`), 5 deps FullCalendar v6. AC1/AC3/AC4/AC5 cumpridos; AC2.1 (resource-timegrid premium) substituído por filtro via `Select` — tech debt registrado em Dev Decisions §1. QA gate **PASS** com 1 concern documentado (resource-timegrid). Smoke em prod (`https://usekeyra.com/agenda`): empty state + toolbar + responsividade + i18n + auth gating todos OK. Commits `b633a44` (po validation), `318c5c1` (dev impl), `c97d2f3` (qa gate Done). |
| 2026-04-30 — Story 2.5 (Agendamento) | **Ciclo SDC completo**: `@dev` implementa, `@qa` aprova, `@devops` push — em uma sessão. 7 arquivos (2 novos, 5 modificados) +771 linhas. Validator novo (`lib/validators/appointment.ts`), 2 Server Actions (`createAppointment`, `listAgendaPickers`), Sheet com `react-hook-form + zod` integrando paciente/serviço/profissional/data/início/duração + preview em tempo real (término, receita prevista, comissão). Pickers carregados no SSR (`page.tsx` em `Promise.all`) — evita `useEffect+setState` no client e elimina loading state. Snapshots `price_snapshot` e `commission_snapshot` calculados server-side (`services.price` + `services.commission_rate ?? professionals.default_commission_rate ?? 0`, ADR-013). EXCLUDE constraint do banco (`appointments_no_double_book` — gist + tstzrange) capturado via SQLSTATE `23P01` e devolvido como toast amigável sem fechar form. `dateClick` em slot vazio do FullCalendar abre form com data pré-preenchida; botão "+ Novo agendamento" no toolbar abre vazio. QA gate **PASS sem concerns** — todas as 3 ACs cumpridas conforme espec. Commits `90aeae6` (dev impl), `bf9f6d9` (qa gate Done). |
| 2026-05-01 — Story 2.6 (Status) | **Ciclo SDC completo em sessão única**: `@dev` implementa → `@qa` PASS+1 concern → `@devops` push (CI verde). 10 arquivos (3 novos, 7 modificados) +821 linhas. Server Action `changeAppointmentStatus(id, to, reason?)` com guard de transição em duas camadas (Zod restringe `to`; check do status atual no banco bloqueia transição partindo de estado incompatível). Matriz: `scheduled → {done, cancelled, no_show}`; `done → cancelled`; `cancelled`/`no_show` read-only. `cancel-dialog.tsx` (AlertDialog reusável + combobox de 4 motivos canônicos + textarea livre, "Outro" exige descrição, composição `motivo: detalhe`). Sheet de detalhes (`event-detail-sheet.tsx`) reescrito: Badge colorido por estado (azul/verde/cinza/âmbar), botões contextuais, AlertDialogs próprios para Concluir e Falta. Trigger `trg_appointment_done_creates_command` continua sendo a fonte da comanda — a action só faz UPDATE; o trigger cria `commands` + `command_items` na mesma transação (`BEFORE UPDATE OF status` com guard `OLD.status IS DISTINCT FROM 'done'`). Audit_log automático via trigger genérico `trg_audit_appointments`. shadcn `alert-dialog` + `textarea` instalados (deps que faltavam). `calendar-client.tsx` propaga callback `onChanged` que faz `refetchEvents()`. Pattern "adjust state during render" no `cancel-dialog.tsx` em vez de `useEffect` (ESLint do React 19 bloqueia `setState` síncrono em effect). QA gate **PASS com 1 concern não bloqueante**: smoke empírico do AC2.2 (gerar comanda ao concluir → ver linha em `commands`) requer execução manual no Supabase Studio — código inspecionado e correto. Commits `b7e4a1e` (feat), `dce81eb` (qa). CI `RLS Tests` run `25226228210` ✅ success. |
| 2026-05-01 — Padrão de sync STATE.md | **Regra estabelecida pela idealizadora:** o `STATE.md` deve ser atualizado ao final de cada Story marcada `Done`, na mesma sessão, antes de seguir para a próxima ou encerrar. Padrão versionado em `CLAUDE.md` (seção "Workflow story-driven", passo 6) e na auto-memória do agente (`feedback_state_md_sync.md`). Sem essa etapa, a próxima sessão lê estado errado. |
| 2026-05-01 — Story 2.7 (Receita prevista) | **Ciclo SDC completo em sessão única**: `@dev` implementa → `@qa` PASS+2 concerns → `@devops` push. 4 arquivos (1 novo, 3 modificados) +178 linhas. `getReceitaPrevista()` em `actions.ts` faz 1 round-trip em `v_receitas_previstas` pelo mês corrente e agrega em TS por buckets `today`/`week`/`month` com `date-fns` pt-BR. Agregação em centavos (inteiros × 100) para preservar precisão dentro de `Number.MAX_SAFE_INTEGER`. `receita-card.tsx` (Server Component sticky no topo com `bg-card/95 backdrop-blur` + `-mx-4 -mt-4`) plugado em `agenda/page.tsx` antes do header. Atualização "automática" via `revalidatePath('/agenda')` chamado pelas mutations de 2.5/2.6 — sem polling. Princípios UX inegociáveis cumpridos. **Concerns**: (C1) smoke empírico em prod pendente; (C2) achado sistêmico — views (`v_receitas_previstas`, `v_dre_*`, `v_cashflow_daily`, `v_dashboard_kpis`) criadas sem `security_invoker=true`, defesa atual é filtro `.eq('org_id', orgId)` na action — sugestão: migration 023 para aplicar em todas. Commits `57a8d2c` (feat), `cd57160` (qa). |
| 2026-05-01 — Concerns C1+C2 zerados | **Resolução completa em sessão única**, antes de avançar para Story 2.3. (C2) Migration `20260501000100_views_security_invoker.sql` aplicada no remoto via `supabase db push` — `ALTER VIEW … SET (security_invoker = true)` em todas as 6 views de leitura. Confirmado via `pg_class.reloptions` no Postgres 17. RLS de `appointments`/`commands`/etc. agora aplica automaticamente em qualquer SELECT — defesa em profundidade real. `database.types.ts` regenerado (sem mudança de shape). (C1) Smoke empírico via psql + `BEGIN ... ROLLBACK` em prod: criou agendamento `scheduled` (R$ 250) → mudou pra `done` → trigger `trg_appointments_done_to_command` gerou comanda `bc7cf634-…` (status=open, subtotal=250) com `command_items` snapshotados (description, quantity=1, unit_price=250, unit_cost=50, commission_rate=0.30) → `done_at` preenchido → view `v_receitas_previstas` confirmou que entrada some quando `status=done`; cancelamento gravou `cancel_reason` + `cancelled_at`; `no_show` transição direta; audit_log capturou tudo via `trg_audit_appointments`. ROLLBACK desfez todas as mutações (0 appointments/customers/services/professionals/commands remanescentes — só audit_log de 4 entries pré-existentes da org). typecheck + lint + build verdes pós-tudo. |
| 2026-05-01 — Story 2.3 (Insumos + BOM) | **Ciclo SDC completo em sessão única — Sprint 2 fechada com 28/28 pontos**. 14 arquivos novos +1673 linhas. Validator `supply.ts` (com `serviceSupplyLinkSchema` parseando vírgula brasileira). Server Actions completas em `estoque/actions.ts`: `listSupplies` paginado, `upsertSupply` com propagação reversa de `unit_cost` para todos os serviços que usam o insumo, `archive`/`unarchive`, `listInventoryMovements` read-only, `listServiceBom`, `attachSupplyToService` com `upsert(onConflict='service_id,supply_id')` + `recalcServiceUnitCost` server-side, `detachSupplyFromService`, `listActiveSuppliesForPicker`. Layout `/estoque` com sub-nav (Insumos · Movimentações), redirect de `/estoque` para `/estoque/insumos`. Lista de insumos espelha padrão Story 2.1, com badge "Recompra" quando `current_stock <= reorder_level`. Movimentações read-only com badges semânticos por tipo (`entry`/`exit`/`adjustment`/`service_consumption`/`loss`). Card "Insumos (BOM)" inserido em `/servicos/[id]` apenas para `type='service'` — `service-bom-editor.tsx` (Client) com otimismo local, custo total em tempo real, picker filtrando insumos já vinculados (alinhado com UNIQUE constraint). Sidebar já tinha item "Estoque" — só faltava a rota. Defesa em profundidade: RLS dupla via filtro `eq('org_id')` + `security_invoker=true` (migration 023). QA gate **PASS com 1 concern não bloqueante** (smoke do BOM em prod). Tech debt registrado: TD-2.3.1 (página de form acessível por viewer; submit rejeita), TD-2.3.2 (recalc reverso em loop TS), TD-2.3.3 (recalc em app-layer, não trigger). Commit `69d8c39`. **Smoke C1 resolvido depois**: psql + ROLLBACK validou todas as 9 etapas (insumo, BOM, recalc, propagação reversa, detach, soft-delete, trigger movement). |
| 2026-05-01 — Drafts Sprint 3 (3.1-3.8) | `@sm` draftou 8 stories da Sprint 3 (Automação Financeira) baseadas em EPIC-0 §Fase 3 + PRD (FR-CO-*, FR-FI-*, FR-CP-*, FR-ES-02). `@po` validou todas com checklist de 10 pontos: GO 8-9/10 em todas, status `Draft → Ready`. Total 32 pontos: 3.1 (Comanda, S, 3 pts), 3.2 (Pagamento, M, 5 pts), 3.3 (Transação visualização, S, 2 pts), 3.4 (Receitas por profissional, S, 3 pts), 3.5 (Despesas, M, 5 pts), 3.6 (Custos fixos vs variáveis, S, 3 pts), 3.7 (Fluxo de caixa, M, 5 pts), 3.8 (Rateio de estoque, S, 2 pts). Commit `98d8c15`. |
| 2026-05-01 — Story 3.1 (Comanda automática) | **Sprint 3 abriu com SDC completo em sessão única.** 6 arquivos novos. Validator `command.ts` + actions completas (`listCommands` paginado com filtro por status, `getCommand` com items inline, `addCommandItem` snapshotando unit_cost/commission do serviço atual, `removeCommandItem`, `applyCommandDiscount` validando ≤ subtotal, `finalizeCommand` bloqueando comanda sem itens, `listActiveServicesForPicker`). UI: `/comandas` lista com sub-nav de filtros (Todas/Abertas/Finalizadas/Pagas), `/comandas/[id]` detalhe com `comanda-edit-form.tsx` (Client + otimismo local + cálculo local somente para feedback). Sidebar ganhou item "Comandas" (ícone Receipt). **Decisão de arquitetura: ZERO cálculo na aplicação** — trigger `trg_command_items_recompute` mantém `commands.subtotal`; `commands.total` é GENERATED ALWAYS AS (subtotal - discount); `command_items.total` idem. App só faz INSERT/DELETE/UPDATE simples; banco cuida de tudo. Smoke empírico via psql + ROLLBACK validou: trigger gera comanda ao status=done, snapshot correto em command_items, subtotal recompõe ao adicionar item (200 → 280), GENERATED total atualiza ao aplicar desconto (280 → 250), volta a 170 ao remover item, finalize muda status open → finalized + finalized_at, audit_log captura 6 entries no ciclo. RLS dupla (filtro explícito + security_invoker da migration 023). QA gate **PASS sem concerns**. typecheck + lint + build verdes. |
| 2026-05-01 — Sprint 3 completa (Stories 3.2-3.8) | **Phase 3 FECHADA — todas as 8 stories Done em uma sessão.** Migration 024 (`seed_chart_on_org_create`) atualiza RPC + backfill: orgs nascem com 6 payment_methods + 3 accounts + 29 categorias. Migration 025 (`professionals.cost_center`). **3.2 Pagamento**: validator + action `registerPayment` com Decimal.js (`fee = round(gross × rate, 2) + fixed`), sheet com preview em tempo real, payments-card.tsx; smoke confirmou trigger criou transaction (credit, gross=200, fee=6.98, net=193.02) e comanda virou paid automaticamente. **3.3 Transação visualização**: `/financeiro` com sub-nav (Transações·Receitas·Despesas·Custos fixos·Fluxo·Categorias), `/financeiro/transacoes` com filtros + tabela paginada + 3 cards de totais; validador Zod movido para lib (regra Next 16: `'use server'` só exporta funções async). **3.4 Receitas por profissional**: ADD COLUMN cost_center; form `/team/profissionais` ganha campo; `/financeiro/receitas` agrupa por profissional E por centro. **3.5 Despesas**: validator + actions CRUD + páginas `/financeiro/despesas` + form com select nativo (evita warning React Compiler). **3.6 Custos fixos vs variáveis**: sem migration nova (schema já tinha `kind='fixed_cost'` e `is_fixed`); `/financeiro/custos-fixos` com média móvel 3 meses + clone idempotente do mês passado; `/financeiro/categorias` lista plano de contas agrupado. **3.7 Fluxo de caixa**: `/financeiro/fluxo-caixa` com saldo + tabela diária + saldo acumulado + projeção via `v_receitas_previstas`; cap 365 dias; sem gráfico. **3.8 Rateio de estoque**: smoke confirmou `_consume_command_inventory` cria movement `service_consumption` ao pagar comanda (qty=-50, unit_cost=5); `listInventoryMovements` aceita `commandId?`; `/estoque/movimentacoes?command=` filtra; ConsumoCard em `/comandas/[id]` mostra insumos consumidos quando comanda paga. **Sprint 3 totalizando 32/32 pontos. Phase 3 ✅.** |
| 2026-05-02 — Hardening pós-Sprint 7 (Sentry produção + Supabase template + repo GitHub↔Sentry) | **Sessão de fechamento das 3 dependências não-código.** (A) **Supabase magic-link em pt-BR aplicado via Management API**: descobri os nomes corretos dos campos (`mailer_subjects_magic_link` snake_case, plural `templates`); primeira tentativa com `mailer_subjects_magiclink` retornou 200 mas Supabase ignorou silenciosamente (campo desconhecido). Segunda tentativa com nomes corretos: subject "Seu link de acesso ao KEYRA" + 4249-char template HTML aplicado, confirmado via GET. (B) **Sentry produção corrigido após 4 redeploys diagnósticos** — eu havia provisionado as envs inicialmente com 3 erros simultâneos (slug invertido org↔project, trailing newline herdado de paste, token formato legado 32-hex chars que retorna 401). Diagnóstico via `vercel logs` da API mostrou erros progressivamente mais específicos: `invalid value 'seal-digital\n' for '--project'` → `invalid value 'seal-digital' (use slug not name)` → `Invalid token (http status: 401)`. Após user gerar token novo formato `sntrys_*` (validado contra `POST /api/0/organizations/seal-digital/releases/` retornando HTTP 201), reprovisionado limpo. **Build atual**: 0 erros Sentry, source maps subindo (release `1cfb1773` no log com 230+ msgs de upload, debug IDs visíveis). (C) **Repo `luizxhgomes/keyra` ↔ Sentry**: verificado já ativo via `GET /api/0/organizations/seal-digital/repos/` — `id=5314448 status=active provider=integrations:github`. UI dela mostrando "0/1 connected" era cache desatualizado. Stacktrace agora linka código fonte ↔ commit no GitHub. (D) **Lição salva como memory permanente** (`feedback_validate_creds_before_provisioning.md`): nunca aceitar credenciais coladas no chat sem testar via API antes de provisionar — custo de 1 curl < custo de 4 redeploys. **Outputs/ adicionado ao `.gitignore`** (artefatos de skill design-md). Todos os deploys produção `state=READY`. |
| 2026-05-02 — Sprint 7 código completo (Stories 7.0+7.0.1+7.0.2+7.1+7.2+7.3+7.4) — **(app)/ desbloqueada em prod** | **Sequência de correções em jornada única após validação manual em mobile descobrir que TODAS as rotas autenticadas explodiam com digest opaco `3213099672`.** 4 commits em main com Vercel deploy success em todos: `8ab06b1` (7.0.1 `withSentryConfig` + source maps + tunnel `/monitoring`; spread condicional em `org`/`project`/`authToken` satisfaz `exactOptionalPropertyTypes`) → `58988d4` (7.0.2 HOTFIX template.tsx — root cause identificado por inspeção estática: `<m.div className="contents">` + transform = UB; template virou passthrough `<>{children}</>` + 7.3 criação de `/mais` mobile-only hub com 9 destinos + `/configuracoes` hub com dados da org/plano/perfil/sair) → `02ec96f` (7.4 três `not-found.tsx` em escopos diferentes + `global-error.tsx` com inline-styles para fallback derradeiro + `scripts/audit-dead-links.sh` Gate G5 da Phase 2.6 que escaneia hrefs estáticos e valida match com `page.tsx` — 38 rotas, 16 hrefs, 0 dead. Encontraria `/mais` e `/configuracoes` ANTES da regressão Sprint 6) → `0f8888f` (7.1 login form ganhou cooldown 60s + botão "Reenviar link" com timer pt-BR via `useEffect+setInterval+useRef`; `onResend()` reusa `signInWithOtpAction` + 7.2 template HTML magic-link em pt-BR com identidade KEYRA — texto-pesado + 1 botão + native font stack + sem CDN/imagens; `supabase/email-templates/README.md` documenta aplicação manual no Dashboard). **Story 7.5 (defensive boundaries) coberta sem código novo** — `(app)/error.tsx` já mantém AppShell renderizado (layout pai persistente), falha de rota não derruba sidebar/bottom nav. **Story 7.6 (home cleanup cosmético) deferido.** Sentry capture viabilizado também na Story 7.0.2 — `instrumentation.ts` agora encontra DSN em prod, source maps já uploadados pelo build do `0f8888f`. **Pendentes (não-código):** (1) validação manual em mobile da idealizadora confirmando que `(app)/*` parou de explodir; (2) aplicação manual do template magic-link.html no Supabase Dashboard (sem isso, mesmo com SMTP Resend customizado, o template continua sendo o default em inglês); (3) conectar repo `luizxhgomes/keyra` no Sentry Settings → Repositories. typecheck + lint + build verdes em todos os 4 commits. Audit dead-links zerado. |
| 2026-05-02 — Story 6.5 (Sidebar 7+2 + FAB contextual) — **Sprint 6 fechada 100%** | **Última da Sprint 6 Done — story enxuta validando Phase 2.5 em ambas as direções.** 2 arquivos modificados (Sidebar.tsx + BottomNav.tsx). **AC1**: `NAV_ITEMS` (9) split em `NAV_PRIMARY` (7 operacionais) + `NAV_SECONDARY` (2: Time + Configurações) com `border-t border-border` separando. Helper `renderItem` evita duplicação. Texto auxiliar "Org switcher..." removido. **AC2**: função pura `getFabAction(pathname)` **exportada** (testabilidade futura) com 6 mapeamentos contextuais (`/pacientes`→`/pacientes/novo`, `/servicos`→`/servicos/novo`, `/financeiro/despesas`→`/financeiro/despesas/nova`, `/estoque/insumos`→`/estoque/insumos/novo`, `/comandas`→`/agenda?novo=1` "gera comanda automática", default→`/agenda?novo=1`). FAB com `href` + `aria-label` dinâmicos. Reformulação consciente do AC3 do plano: tooltip Radix → aria-label dinâmico (BottomNav é mobile-only, sem hover real). **Phase 2.5: segunda story a herdar a regra com gates limpos** — diferentemente da 6.2 (que exigiu P1-P5 com 7 riscos), a 6.5 ficou enxuta (S 3 pts) sem pre-work porque G1-G4 retornaram zero. **Os gates filtram, não acrescentam — diagnosticam complexidade real.** typecheck + lint + build verdes. 4 greps DoD (NAV_PRIMARY/SECONDARY=4, getFabAction=3, border-t=2, anti-grep G1=0) todos OK. Self-QA gate **PASS sem concerns**. Phase 3.5: 3 gates WAIVED. **Sprint 6 fechada 100% (25/25 pts) — DS editorial + Motion + Navegação contextual + Gates permanentes.** |
| 2026-05-02 — Story 6.2 (Sistema de Motion + painel visual interativo) | **Story Done após escopo expandido pela idealizadora** (de 8 para 12 momentos canônicos com sombras + pulsos + scroll animation + espaçamentos preservados). 13 arquivos (8 modificados + 5 novos: lazy-motion.tsx provider, tokens.ts com 9 variants, use-motion-preference.ts hook SSR-safe, scroll-fade-rise.tsx helper, (app)/template.tsx). Pre-implementação P1-P5 cumpriu protocolo Phase 2.5 (eat your own dog food — primeira story a herdar a regra da 6.0): G1-G4 aplicados durante draft com decisão documentada para cada um. **framer-motion 12.x instalado** com LazyMotion+domAnimation+strict (`<m.div>` em vez de `<motion.div>`). **tokens.ts** com `satisfies Record<string, Variants>` em vez de cast — preserva tipos literais para `exactOptionalPropertyTypes` que rejeitaria `Variants \| undefined`. **10 momentos implementados**: AnimatePresence mode="wait" no KPICard (P5 protege CON-UX-01 contra morphing de dígitos); subtleScale no StatusBadge; criticalEntrance unificada (slideInDown 300ms + pulseOnce 1500ms via transition por propriedade) no AlertCard severity=critical; routeTransition em template.tsx novo; fadeRiseSlow no EmptyState; ScrollFadeRise helper com whileInView+once: true em 4 cards do dashboard; hover:shadow-md no KPICard; toast sucesso re-tematizado para sálvia via `--success-bg: hsl(var(--secondary))` em globals.css; reduced-motion em 2 camadas (CSS @media + framer-motion useReducedMotion). **Anti-grep ZERADO** — `animate=.*\b(margin\|padding\|width\|height)\b` retorna 0 matches; toda animação usa `transform`. **AC2.3 e AC2.5 deferidos** para Story 6.2.1 com justificativa técnica: AC2.3 (stagger fields RHF — alta superfície vs ganho marginal); AC2.5 (Sidebar layoutId exige domMax features +30KB que viola cap). typecheck + lint + build verdes. 8 greps DoD todos OK. Self-QA gate **PASS com 3 concerns não-bloqueadores**: C1 (bundle delta ~100KB vs cap 30KB — framer-motion 12.x mais pesado que estimado, aceito para MVP); C2 (AC2.3 deferido); C3 (AC2.5 deferido). Phase 3.5: 3 gates WAIVED. |
| 2026-05-02 — Story 6.0 (Higienização pré-motion + gates anti-regressão) | **Story estrutural Done após auditoria `@baziotti` 7.6/10 detectar 4 HIGH escapados das stories 6.1/6.3/6.4.** Análise de causa-raiz pelo orquestrador identificou raiz comum: falta de gates cross-cutting durante drafts. Idealizadora cobrou: "isso é inadmissível, precisa prever para que não aconteça". 16 arquivos modificados em sessão única. **AC1**: 3 alertas reescritos em valores absolutos (princípio CON-UX-01 violado desde Story 4.9, agora corrigido — `formatBRL(absoluteDrop)` em vez de `${dropPct.toFixed(0)}%`). **AC2**: sweep cromático cuidadoso 14× `text-emerald-700` → `text-lucro` em 8 arquivos (perl batch + reversão manual do badge "Top" em `dre-por-servico:95` que o sweep cego destruiria). **AC3**: `min-h-[44px]` em action e secondaryAction do `<AlertCard>` (regressão da Sprint 5.4 corrigida — touch target abaixo de AA não tem waiver tácito). **AC4**: banner truncamento da agenda → `<AlertCard severity="warning">` (fonte única real no app). **AC5**: 3 cosméticos (`p-card-y` → `px-card-x py-card-y` em ErrorMessage; remoção de padding duplicado em agenda; ícone `PieChart` → `Coins` em DRE-por-servico). **AC6**: comentários JSDoc em `dre/page.tsx` (coluna `% sobre receita` mantida como ratio analítico, decisão da idealizadora) e `dashboard/page.tsx` (ordem KPIs hero → Alertas → grid 2 cols → Meta documentada). **AC7 ESTRUTURAL — instaura Phase 2.5 Anti-Regression Gates em `.claude/rules/story-lifecycle.md` como regra permanente do projeto**: G1 (princípios inegociáveis — greps `\.toFixed\(\d+\)%`), G2 (inventário de tokens — grep do padrão antigo em todo codebase ANTES de escrever ACs), G3 (touch target AA 44×44 mínimo — sem waiver tácito), G4 (fonte única real — refatorar local não é fonte única). Protocolo de gate failure: expandir escopo / story irmã / waiver explícito justificado. **Eat your own dog food**: gates aplicados na própria 6.0 antes dos ACs detectaram exatamente o inventário esperado. **AC8**: J1-J6 todas validadas — J3 sem fragmentação cromática, J5 com copy absoluta + toque AA. typecheck + lint + build verdes. 6 greps DoD todos OK. Phase 3.5: Financial Gate APROVADO (verificação aritmética em PO — zero alteração em fórmula); Compliance/Growth WAIVED. Self-QA gate **PASS**; 1 concern não-bloqueante (contraste `text-lucro` sobre `bg-muted/30` calculado ~4.2:1 marginal AA — smoke pós-deploy valida). |
| 2026-05-02 — Story 6.3 (Alert hierarchy 3+5+5 + dismiss + fonte única) | **Terceira da Sprint 6 Done em sessão única — implementação com pre-work rigoroso.** Ciclo SDC: `@sm *draft 6.3` (escopo dual: refactor de fonte única + dismiss) → idealizadora alerta para riscos antecipados → `@sm *draft 6.3 — revisão` adiciona seção "Pre-implementação (correções de risco)" com 4 pre-works (P1-P4) endereçando os 5 riscos antes dos ACs principais → `@po *validate-story-draft` GO 10/10 + Phase 3.5 com 3 gates WAIVED → `@dev *develop-story 6.3` implementa em ordem estrita: **P1**: `<AlertCard>` ganha prop opcional `secondaryAction?: { label, onClick }` (compatibilidade reversa preservada). **P2**: `lib/storage.ts` NOVO — utility `safeLocalStorage` com 5 métodos (getItem/setItem/removeItem/getJSON/setJSON), todos try/catch para SSR + incognito iOS + quota cheio. **P3**: `lib/hooks/use-dismissed-alerts.ts` NOVO — hook via `useSyncExternalStore` (pattern canônico React 18+), server snapshot retorna `EMPTY_SNAPSHOT` const compartilhado, client lê localStorage com cleanup on-read, subscribe a `storage` event + dispatch manual de StorageEvent na própria aba (browsers não emitem `storage` na origem da escrita por design), `useMemo` em `dismissedIds` (correção pós-lint para `react-hooks/exhaustive-deps`). **P4**: `space-y-stack-loose` (24px) no `<ul>` da AlertasList — respiração mobile sem voltar ao border-l-4. typecheck verde após pre-work antes de tocar AlertasCard. **AC1**: `AlertasCard` (Server) refatorada de 60+ para 26 linhas — busca dados + delega para `<AlertasList>` (Client NOVO). Removidos: mapas `SEVERITY_BORDER`/`SEVERITY_ICON`, `<StatusBadge>` redundante, estrutura `<ul>/<li>` com `border-l-4`, `<AlertTriangle>` inline, `<Link>` "Investigar". **AC2**: cap 3+5+5 via reduce imutável (compatível React Compiler), SEVERITY_ORDER critical→warning→info, linha "Mais N silenciados pelo limite" condicional (defensivo Phase 5 — atualmente nunca atinge com 4 regras). **AC3**: secondaryAction com handler `dismiss(a.id)` do hook (P3), omitido com spread condicional para `severity === 'critical'` (proteção do sinal de risco — perda de risco proibida). **AC4**: 2 variações empty state — `CheckCircle2` "Nenhum alerta esta semana" preservado da 5.7 + `BellOff` "{N} alertas silenciados" com tom neutro (não positivo — "você decidiu adiar"). **AC5**: J1-J6 todas validadas mentalmente — J5 (estoque) com warning silenciável; J3 (fechamento) com critical não-silenciável. typecheck + lint + build verdes (33 routes). 4 greps DoD todos OK: 0 `SEVERITY_BORDER` reais / 1 uso `<AlertCard>` / 7 `useDismissedAlerts` / 8 `safeLocalStorage`. Self-QA gate **PASS** sem bloqueadores; 1 concern não-bloqueante (C1: touch target do botão Silenciar ~22px abaixo de AA 44px — decisão deliberada para hierarquia de proeminência; escalar para `<Button size="sm">` se smoke real revelar problema). |
| 2026-05-02 — Story 6.4 (Refactor visual DRE + sweep H2) | **Segunda da Sprint 6 Done em sessão única.** Ciclo SDC: `@sm *draft 6.4` (escopo dual: DRE refactor + concern C1 da 6.1, com 7 ACs e Phase 3.5 explicitada) → `@po *validate-story-draft` GO 10/10 + Financial Gate **WAIVED** (story visual-only, zero fórmula tocada) → `@dev *develop-story 6.4` implementa. **AC1**: 10 H2s migrados via perl batch para `text-h2` (9× `text-lg font-semibold` financeiros + 1× `text-xl font-semibold tracking-tight` em estoque/insumos/[id]). **AC2-AC4**: `dre/page.tsx` ganhou refactor cirúrgico — `border-t-2 border-border` em 2 linhas (subtotal + final), subtotal `revenueNet` com `bg-muted/30 text-h3` (font-semibold removido — token tem 600), total `netProfit` com `bg-muted/50 text-h2` + cor por sinal aplicada à linha inteira (`text-secondary` sálvia se ≥0, `text-destructive` < 0). **AC5**: 48 ocorrências `py-stack-loose` nos 3 DREs (sweep perl + 3 ajustes manuais em template literals com backtick). **AC6**: `custos-fixos/page.tsx` célula "Variação" refatorada — div custom com `<ComparativoTexto delta={cents} period="média móvel" sentiment={...} />` substituindo string inline; sentiment captura semântica (custo caindo = `positive`, subindo = `negative`); fallback "sem histórico". **AC7**: J1-J6 todas validadas; J3 com critério "< 3s para identificar resultado" cumprido mentalmente. typecheck + lint + build verdes. Greps DoD: 0 H2 antigos / 12 `text-h2` / 2 `border-t-2`. Self-QA gate **PASS** sem concerns bloqueadores; 1 concern não-bloqueante (C1: `dre-por-servico` e `dre-por-profissional` ainda usam `text-emerald-700` para lucro positivo — alinhamento com `text-secondary` fica para Sprint 6 follow-up). |
| 2026-05-02 — Story 6.1 (Tipografia editorial) | **Primeira da Sprint 6 (camada editorial) Done em sessão única.** Ciclo SDC: `@sm *draft 6.1` (6 ACs com greps no DoD + Out of Scope explícito) → `@po *validate-story-draft` GO 10/10 + Nota 2 sobre sintaxe Tailwind 3.4 → `@dev *develop-story 6.1` implementa. **AC1**: Inter Variable confirmada — `axes: ['opsz']` testado e descartado após bundle subir 224→348KB (+124KB) sem ganho perceptual; Inter wght-only via `next/font/google` cobre 100–900 sem custo extra, paridade exata com Sprint 5.7. **AC2**: 10 tokens fontSize em `tailwind.config.ts` (display-hero 56px/200, display 40px/200, h1 32px/600, h2 24px/600, h3 20px/600, kpi-hero 56px/600, kpi 40px/600, body 16px/400, body-sm 14px/400, label 12px/600 + tracking 0.05em). **AC3**: aplicação em 48 lugares — 2× dashboard H1 → `text-display-hero`; 12× outras H1 → `text-display` (agenda, pacientes ×3, comandas ×2, financeiro, team, estoque, servicos ×3); KPICard variants hero/secondary → `text-kpi-hero`/`text-kpi`; Card.tsx CardTitle → `text-h3`; 14× labels uppercase tracking-wide → `text-label uppercase` (sweep amplo via perl batch incluiu event-detail-sheet, agendamento-form ×3, dropdown-menu, error.tsx). **AC4**: button.tsx base com `font-semibold` (todas variantes). **AC5**: bundle 224KB → 224KB paridade; greps obrigatórios 3/3 OK. **AC6**: J1-J6 todas validadas. typecheck + lint + build verdes. Self-QA gate **PASS** sem concerns bloqueadores; 2 concerns não-bloqueantes registrados: C1 (H2 de cabeçalho de seção fica para 6.4) e C2 (validação de campo do display 200 em mobile real iPhone SE/Galaxy A). |
| 2026-05-01 — Story 5.7 (Higienização de cobertura) | **Sprint 5 fechada de verdade após revalidação `baziotti` que apontou 6.8/10 com 9 itens HIGH escapados.** Ciclo SDC completo em sessão única: `@sm *draft 5.7` (10 itens bloqueantes mapeados arquivo-a-arquivo + greps objetivos no DoD) → `@po *validate-story-draft 5.7` GO 10/10 + seção "Out of Scope" → `@dev *develop-story 5.7` implementa 19 arquivos modificados (0 novos). **AC1**: ErrorMessage em 4 páginas (despesas, transacoes, categorias, custos-fixos). **AC2**: EmptyState em 15 lugares (1 a mais que a lista inicial — cap 365 dias do fluxo-caixa também virou EmptyState com `Clock`). **AC3**: AlertasCard removido `border-blue-500` + `text-blue-700` inline → `border-info` (token semântico KEYRA `#8A7A6B`) + `SEVERITY_ICON` map; `null` retorno substituído por empty state positivo "Nenhum alerta esta semana — sua operação está sob controle" com `CheckCircle2`. **AC4**: BottomNav FAB virou `<Link href="/agenda?novo=1">`; `calendar-client.tsx` usa `useSearchParams` + pattern "adjust state during render" (regra `react-hooks/set-state-in-effect` do React 19 bloqueou setState em useEffect na primeira tentativa); `handleFormOpenChange` limpa `?novo` ao fechar para permitir reabertura. **AC5**: KPICardSkeleton tokenizado (`px-card-x-hero py-card-y-hero` para hero, `px-card-x py-card-y` para secondary). **AC6**: validação mental das 6 jornadas todas verdes — Camila não encontra mais linguagem velha em nenhuma. typecheck + lint + build verdes. 4 greps de cobertura zerados. Self-QA gate **PASS** sem concerns bloqueadores; 1 concern não-bloqueante (C1) registrado: dashboard com `noData=true` ainda mostra IndicadoresCard com EmptyState ao lado do noData EmptyState — fica para Sprint 6.5 (welcome card "estado dia 1"). |

> **Quando atualizar:** ao encerrar cada Story, cada Sprint ou ao bater um bloqueador novo. Fonte de verdade operacional, complementar ao `IMPLEMENTATION-MAP.md` (tático) e ao `EPIC-0` (estratégico). Regra de obrigatoriedade pós-Story Done formalizada em 2026-05-01 (CLAUDE.md §"Workflow story-driven" passo 6).

