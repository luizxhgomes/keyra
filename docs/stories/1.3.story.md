# Story 1.3: Gestão de Profissionais + Convites por Email + Roles UI

## Status

InReview

## Complexidade

**T-shirt size:** M (~8 pontos · estimado em 1,5–2 dias de trabalho de @dev)

Racional: 3 sub-features independentes (profissionais CRUD, convites, roles) que compartilham Server Actions e layout comum; schema já existe; templates de email são React puro; RPC de aceite é o único SQL novo.

## Story

**Como** owner ou admin de uma organização KEYRA,
**Eu quero** cadastrar profissionais da clínica, convidar membros para o app por email e administrar seus papéis,
**Para que** eu possa montar o time sem depender de uploads manuais e delegar permissões com clareza (owner · admin · professional · viewer).

## Contexto

Hoje a organização é criada no onboarding e o usuário que criou vira automaticamente `owner`. Para crescer, faltam três capacidades:

1. **Cadastro de profissionais** — a tabela `professionals` já existe e é usada futuramente em `appointments.professional_id`. Um profissional pode ou não ter um `user_id` do Supabase Auth: uma recepcionista que marca consulta é um profissional sem acesso ao app; a dona da clínica é um profissional **com** acesso.
2. **Convite de membros por email** — a tabela `organization_invites` existe mas não é usada por UI nenhuma. O convite deve gerar token único, expirar em 72 h e disparar email via Resend (ADR-021).
3. **Administração de roles** — `memberships.role` aceita `owner | admin | professional | viewer`. Falta a UI para promover, rebaixar e remover, respeitando invariantes (não rebaixar último owner, admin não mexe em owner, etc.).

Isto fecha a Phase 1 de Fundação Técnica (junto com a Story 1.4).

## Acceptance Criteria

### AC1 — CRUD de profissionais

1. Existe página `/team/profissionais` protegida por `requireAuth()` + role `owner | admin`.
2. A página lista todos os profissionais ativos da `current_org` com colunas: nome, email (opcional), vínculo de acesso (com ou sem membership), status (ativo/inativo).
3. Ação **"Novo profissional"** abre form com: nome (obrigatório), email (opcional), especialidade (opcional), cor (opcional — agenda), `default_commission_rate` (opcional, fração 0–1 — campo aceita 0 a 100 em %, persistido /100).
4. Ação **"Editar"** permite mudar os mesmos campos.
5. Ação **"Desativar"** faz soft-delete (`deleted_at = now()`), sem remover registros dependentes (appointments históricos).
6. Qualquer usuário com role `professional` ou `viewer` que navegue para `/team/profissionais` recebe 403 (redirect para `/dashboard` com toast).

### AC2 — Convite de membro por email

1. Existe página `/team/convites` protegida por role `owner | admin`.
2. Ação **"Convidar membro"** abre form com: email (obrigatório, válido), role (`admin | professional | viewer` — nunca `owner`).
3. Ao submeter, cria registro em `organization_invites` com token hex 48-char (default do schema via `encode(gen_random_bytes(24), 'hex')`), `expires_at = now() + interval '72 hours'` (override do default de 14 dias), `invited_by = current_user.id` e dispara email via `sendEmail` (ADR-021).
4. O email contém: saudação com nome da org, texto explicando o papel convidado, botão `Aceitar convite` apontando para `https://usekeyra.vercel.app/invites/{token}`, rodapé com instrução de ignorar.
5. A página `/team/convites` lista convites pendentes (não aceitos, não expirados) com: email, role, data do envio, tempo restante, ações **Reenviar** (regera token + reseta expires_at + reemite email) e **Revogar** (hard-delete, porque a tabela não tem `deleted_at`).
6. Não é possível convidar o mesmo email para a mesma org duas vezes enquanto houver convite pendente (validação server-side + toast claro).
7. Após 72 h sem aceite, o convite é considerado expirado e aparece em lista separada (ou com badge "expirado"), sem permitir aceitação posterior.

### AC3 — Rota de aceite `/invites/[token]`

1. Rota pública (sem `requireAuth`), acessa qualquer `token`.
2. Se o token não existe, está revogado, expirado ou já consumido: renderiza página de erro amigável com link para `/login`.
3. Se o token existe mas o usuário não está logado: redireciona para `/login?next=/invites/{token}` — após login volta e tenta aceitar.
4. Se o usuário está logado mas `auth.user.email !== invite.email`: mostra erro "Este convite foi enviado para outro email. Faça login com a conta correta." + botão sign-out.
5. Se tudo bate: cria `memberships(org_id, user_id, role)` via RPC transacional, marca `organization_invites.accepted_at = now()`, atualiza `user_preferences.current_org_id = invite.org_id` e redireciona para `/dashboard` com toast "Bem-vindo ao {nome_org}".
6. Aceitar convite não pode quebrar RLS do usuário em outras orgs onde ele já tenha membership.

### AC4 — Administração de roles e membros

1. A página `/team` (índice do time) mostra duas listas: **Membros** (memberships) e **Convites** pendentes.
2. Para cada membro há badge de role, data de entrada e (para owner/admin) ações: **Promover**, **Rebaixar**, **Remover**.
3. Regras de invariante (enforced server-side via Server Action, não só na UI):
   - Apenas `owner` pode promover outro membro a `owner` (delegação de dono).
   - `admin` pode promover `professional ↔ viewer` mas não pode mexer em `owner`.
   - Nunca pode existir 0 owners em uma organização (a action falha com mensagem clara se for a última demoção).
   - Remover um membro faz soft-delete de `memberships` + invalida a sessão se for o próprio usuário removido (opcional — aceitável que só invalide no próximo refresh).
4. Toda mudança de role gera registro em `audit_log` (trigger já existe).

### AC5 — Segurança e compliance

1. Todas as Server Actions fazem `requireAuth()` + verificação explícita de role antes de executar.
2. Nenhuma action aceita `org_id` do cliente — usa sempre `current_org_id` do JWT/fallback.
3. Email de convite não contém informação sensível além do nome da org e do papel.
4. Token do convite tem entropia suficiente (UUID v4 — já default no Postgres).
5. Rate limit em Server Actions de convite: máx. 10 por minuto por `org_id`.

### AC6 — UX e visual

1. Estados vazios têm cópia amigável: "Ainda sem profissionais cadastrados", "Nenhum convite pendente".
2. Formulários usam `react-hook-form` + `zodResolver` + toasts Sonner no padrão da Story 1.2.
3. Layout segue componentes canônicos já existentes (`Card`, `Button`, `Input`, `Label`, `Badge`, `DropdownMenu`).
4. Mobile: ações ficam em sheet ou dropdown (sem quebrar em telas ≤ 375 px).
5. Copy inteiramente em pt-BR com acentuação correta (regra CLAUDE.md).

## Tasks / Subtasks

- [ ] **Schema & helpers**
  - [ ] Confirmar que `organization_invites` tem coluna `token UUID DEFAULT uuid_generate_v4()` e índice único em `(org_id, email)` WHERE `accepted_at IS NULL AND deleted_at IS NULL`; se não, criar migration
  - [ ] Criar RPC `accept_organization_invite(_token UUID)` que valida + cria membership + marca aceite atomicamente
  - [ ] Adicionar helpers em `apps/web/src/lib/auth/roles.ts`: `canManageMembers(role)`, `canPromoteTo(currentRole, targetRole)`, etc.

- [ ] **Server Actions**
  - [ ] `app/(app)/team/actions.ts`: `createProfessional`, `updateProfessional`, `deactivateProfessional`
  - [ ] `app/(app)/team/actions.ts`: `createInvite`, `resendInvite`, `revokeInvite`
  - [ ] `app/(app)/team/actions.ts`: `promoteMember`, `demoteMember`, `removeMember`
  - [ ] `app/invites/[token]/actions.ts`: `acceptInvite`
  - [ ] Cada action com validação Zod de entrada

- [ ] **Templates de email**
  - [ ] `apps/web/src/emails/invite-email.tsx` (React Email) com props: `orgName`, `inviterName`, `role`, `inviteUrl`
  - [ ] Teste visual via `react-email dev` (opcional mas recomendado)

- [ ] **Páginas e componentes**
  - [ ] `app/(app)/team/layout.tsx` — sub-navegação: Membros | Profissionais | Convites
  - [ ] `app/(app)/team/page.tsx` — membros + convites pendentes (dashboard do time)
  - [ ] `app/(app)/team/profissionais/page.tsx` + `profissional-form.tsx`
  - [ ] `app/(app)/team/convites/page.tsx` + `convite-form.tsx`
  - [ ] `app/invites/[token]/page.tsx` — aceite do convite

- [ ] **Sidebar**
  - [ ] Adicionar item "Time" em `components/layout/Sidebar.tsx` (ícone Users, rota `/team`) visível para `owner | admin` apenas

- [ ] **Testes manuais**
  - [ ] Fluxo feliz: owner convida email → aceita logado → vê dashboard na nova org
  - [ ] Fluxo não logado: clica no link do email, faz login, aceita automaticamente
  - [ ] Fluxo email errado: usuário logado com email diferente vê erro
  - [ ] Fluxo revogar: owner revoga convite, tentar aceitar devolve 404
  - [ ] Fluxo expiração: manualmente setar `expires_at` no passado, tentar aceitar devolve erro
  - [ ] Fluxo roles: owner promove admin, admin tenta promover owner (bloqueado), último owner tenta rebaixar a si (bloqueado)

- [ ] **Qualidade**
  - [ ] `pnpm typecheck` verde
  - [ ] `pnpm lint` verde
  - [ ] `pnpm build` verde
  - [ ] `pnpm dev` smoke test manual cobre os 6 fluxos acima

## Dev Notes

- **Resend dry-run:** enquanto o domínio `keyra.app` não estiver verificado em Resend, `sendEmail` pode cair em dry-run (env `RESEND_API_KEY` ausente). O aceite funciona igualmente — basta o tester copiar o `inviteUrl` do `console.info` no log do Vercel/local.
- **Evitar elevação de privilégio:** toda action de role verifica se `current_user.id` tem `role IN ('owner', 'admin')` na `current_org` **e** aplica a regra específica de "quem pode promover a quê". Não confiar em `hidden fields` do form.
- **Race nas aceitações:** se dois tabs do mesmo usuário abrirem o link simultaneamente, a RPC `accept_organization_invite` deve ser idempotente — o segundo retorna "já aceito" sem erro.
- **Onboarding vs Convite:** um usuário convidado que ainda não existe em `auth.users` precisa criar conta. O fluxo é: clica no link → `/login?next=/invites/{token}` → magic link → volta → aceita. Não precisamos suportar "aceite sem conta" — fora do escopo.
- **Sign-out após remoção:** remover um membro que está logado não invalida o cookie imediatamente. Aceitável: na próxima chamada a `requireAuth()` ele cai em "membership não encontrada" e vai para `/onboarding/nova-organizacao` (ou `/login` se não tiver outra org).

## Dependencies

- Story 1.1 (scaffold) ✅
- Story 1.2 (auth + onboarding) ✅
- ADR-010 (Supabase Auth), ADR-011 (RLS), ADR-012 (JWT org_id), ADR-021 (Resend) ✅
- Schema: `professionals`, `memberships`, `organization_invites` ✅
- `lib/email/send.ts` ✅ (criado nesta iteração)

## Risks

| # | Risco | Mitigação |
|---|-------|-----------|
| 1 | Email cai em spam sem DNS verificado | Manter dry-run até domínio verificado; usar `onboarding@resend.dev` para testes internos |
| 2 | Promoção a owner acidental | UI confirma com modal; action exige `current_role === 'owner'` |
| 3 | Token leaked via logs | `sendEmail` não loga o token na URL; apenas o domínio em dry-run |
| 4 | RLS não cobre `organization_invites` direito | Teste explícito na Story 1.4 (pgTAP) |

## Definition of Done

1. Todos os ACs passam em teste manual no ambiente de dev.
2. `pnpm typecheck`, `pnpm lint`, `pnpm build` verdes.
3. Os 6 fluxos testados (happy, logged out, wrong email, revoke, expire, roles) com capturas de tela em comentário do PR.
4. `STATE.md` e `IMPLEMENTATION-MAP.md` atualizados com status `1.3 ✅`.
5. Push para `main` via @devops dispara deploy automático em `usekeyra.vercel.app`.

## QA Gate Summary (2026-04-20)

| # | Check | Status | Observação |
|---|-------|--------|------------|
| 1 | Code review (padrões, legibilidade) | PASS | Segue padrões de Story 1.2: Server Actions `'use server'`, Zod em toda entrada, toasts Sonner, shadcn/ui, separação server/client limpa (roles.ts vs roles-shared.ts). |
| 2 | Testes unitários / suíte | N/A | Phase 1 não exige unit tests — Story 1.4 entrega a suíte RLS. Testes manuais documentados nas Tasks. |
| 3 | Acceptance Criteria | PASS | Todos os 6 ACs cobertos no código. AC3 depende da RPC `accept_organization_invite` estar aplicada no Postgres. |
| 4 | Regressões | PASS | Build gera 10 rotas (vs 5 antes); rotas existentes (/login, /dashboard, /onboarding, /auth/callback) preservadas; Sidebar ganhou item "Time" sem remover anteriores. |
| 5 | Performance | PASS | Nenhuma query adicional no caminho crítico; formulários usam `useTransition`; dropdown lazy-loads ações. |
| 6 | Segurança | PASS | Toda Server Action faz `requireAuth()` + `requireRole()`. `org_id` nunca vem do client. Email do convite não expõe dados além de nome da org e papel. RPC de aceite valida JWT email internamente. |
| 7 | Documentação | PASS | ADR-021 criado, story atualizada, migration documentada com comentários, helpers bem comentados. |

**Verdict:** CONCERNS — aprovado condicionalmente. Para fechar (`Done`), o usuário precisa:

1. Aplicar migration: `supabase db push` (ou MCP `apply_migration`)
2. Regenerar types: `pnpm --filter @keyra/web typegen` — depois disso, os dois `as never` em `actions.ts` (chamadas `rpc('count_org_owners'|'accept_organization_invite')`) podem virar chamadas tipadas
3. Rodar os 6 fluxos manuais listados em Tasks / Subtasks > "Testes manuais"
4. Se tudo OK → push para `main` via `@devops *push`

Se qualquer fluxo falhar, voltar para Ready e corrigir.

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-04-20 | @sm (Orion atuando) | Draft criado |
| 2026-04-20 | @po (Orion atuando) | Validação 10-point: GO (10/10). Status Draft → Ready. |
| 2026-04-20 | @dev (Orion atuando) | Implementação concluída (15 arquivos novos, typecheck + lint + build verdes). Status Ready → InReview. Requer `supabase db push` para aplicar migration `20260420000100_invite_accept_rpc.sql` antes do smoke test real. |
| 2026-04-20 | @qa (Orion atuando) | QA gate: CONCERNS — PASS em 6 dos 7 checks; teste manual pendente pelo usuário após aplicar migration no remoto. |
