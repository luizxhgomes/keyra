# Story 2.4: Agenda — Visualização (FullCalendar)

## Status

InReview

## Story

**Como** owner/admin/professional,
**Eu quero** ver a agenda de toda a clínica ou filtrada por profissional, em visualizações dia/semana/mês,
**Para que** eu tenha contexto visual antes de marcar novos agendamentos.

## Complexidade

**T-shirt:** L (~10 pontos · 2 dias) — Story não-trivial por integrar biblioteca externa com Server Components + drag-and-drop.

## Contexto

ADR-002 escolhe FullCalendar (`@fullcalendar/react` + plugins `daygrid`, `timegrid`, `resource-timegrid`, `interaction`). Schema `appointments` já tem EXCLUDE constraint contra double-book. Esta story entrega apenas **visualização**; agendamento (criar/editar) é a Story 2.5.

## Acceptance Criteria

### AC1 — Render

1. `/agenda` protegida por viewer+; desktop mostra visualização semana por default, mobile mostra dia.
2. Toggle dia/semana/mês no topo.
3. Navegação: hoje, anterior, próximo.
4. Dado `professional_id` via query param, filtra; senão mostra todos.

### AC2 — Recursos

1. View "resource-timegrid" (semana com coluna por profissional) quando filtro "todos".
2. Cor do evento vem de `appointments.color` ou fallback para `service_category.color`.

### AC3 — Eventos

1. Cada evento exibe: nome do paciente, serviço, horário.
2. Clicar abre sheet com detalhes e botões (editar — Story 2.5+; concluir — Story 2.6; cancelar — Story 2.6).
3. Status "cancelled" aparece com opacity 0.4.

### AC4 — Performance

1. Query server filtra por `starts_at BETWEEN range` + `org_id` (RLS cuida do resto).
2. Carrega ≤ 200 eventos por view. Paginação não aplicável (time-bound).

### AC5 — Mobile

1. Layout timegrid funcional no mobile (≥ 375px).
2. Toque longo em slot vazio abre "novo agendamento" (Story 2.5).

## Tasks / Subtasks

- [x] Instalar `@fullcalendar/react` + plugins daygrid/timegrid/interaction (resource-timegrid removido — premium, ver Dev Decision abaixo)
- [x] `app/(app)/agenda/page.tsx` — server shell com auth/role gate + lista de profissionais
- [x] `app/(app)/agenda/calendar-client.tsx` — Client Component com FullCalendar (locale pt-br, eventos via callback assíncrono, sheet integrado)
- [x] `app/(app)/agenda/agenda-toolbar.tsx` — toolbar customizado (toggle dia/semana/mês + filtro profissional + nav)
- [x] `app/(app)/agenda/actions.ts` — Server Actions `listAppointments(range, professionalId?)` e `listAgendaProfessionals()`
- [x] `app/(app)/agenda/event-detail-sheet.tsx` — sheet com detalhes do agendamento + botões placeholder pra Stories 2.5/2.6
- [x] Adicionar shadcn `sheet` e `select` (componentes que faltavam)

## File List

- `apps/web/src/app/(app)/agenda/page.tsx` (novo) — Server Component shell
- `apps/web/src/app/(app)/agenda/actions.ts` (novo) — Server Actions
- `apps/web/src/app/(app)/agenda/calendar-client.tsx` (novo) — FullCalendar client
- `apps/web/src/app/(app)/agenda/agenda-toolbar.tsx` (novo) — toolbar custom
- `apps/web/src/app/(app)/agenda/event-detail-sheet.tsx` (novo) — sheet de detalhes
- `apps/web/src/components/ui/sheet.tsx` (novo, shadcn)
- `apps/web/src/components/ui/select.tsx` (novo, shadcn)
- `apps/web/package.json` (deps): `@fullcalendar/react`, `@fullcalendar/core`, `@fullcalendar/daygrid`, `@fullcalendar/timegrid`, `@fullcalendar/interaction`

## Dev Decisions (registradas durante a implementação)

1. **`resource-timegrid` removido** — o plugin é **premium** desde FullCalendar v6. AC2 ("view resource-timegrid quando filtro = todos") não é entregável no plano Hobby sem licença. Substituição: filtro por profissional via Select no toolbar, eventos coloridos por profissional/categoria. Tech debt: avaliar licença ou alternativa (ex.: react-big-calendar) numa Story de polish quando o produto justificar a despesa.
2. **Sem plugin de timezone** — `@fullcalendar/luxon3` adicionaria peso ao bundle. Para o mercado brasileiro, FullCalendar usa o TZ do navegador, que padrão é `America/Sao_Paulo`. Tech debt para quando entrarem clínicas em fuso diferente.
3. **Cap de 200 eventos** — query usa `limit(201)` e detecta truncate comparando length. Banner amber-50 não-bloqueante sugere refinar filtro/range.
4. **Mobile default** — feito via `useEffect` chamando `calendar.changeView('timeGridDay')` (sem setState dentro do effect, alinhado com a regra `react-hooks/set-state-in-effect` do React 19). State sincroniza via `datesSet` callback.

## Dependencies

- Stories 2.1, 2.2 (precisamos de pacientes e serviços para exibir)
- Schema `appointments` ✅

## Dev Notes (PO — adicionado em validação 2026-04-30)

- **Timezone canônico:** `America/Sao_Paulo`. Configurar `timeZone` no FullCalendar; nunca confiar em horário do navegador para gravar `starts_at`/`ends_at` (Postgres usa `timestamptz`).
- **Locale pt-BR:** importar `@fullcalendar/core/locales/pt-br` e setar `locale: 'pt-br'` no calendar config (mês/dia/toolbar em português).
- **Role guard mínimo:** layout `/agenda` deve chamar `requireRole('viewer')` (admin/owner/professional/viewer todos podem ler).
- **Cap de 200 eventos por view:** se `count(appointments) > 200` no range carregado, exibir banner não-bloqueante "Mais de 200 agendamentos no período — refine o filtro de profissional ou diminua a faixa." (não truncar silenciosamente — usuário precisa saber).
- **Mobile <375px:** aceitar overflow horizontal com scroll. Não tentar reflow agressivo — princípio UX KEYRA: clareza > tentar caber tudo.
- **Performance:** view `resource-timegrid` é a mais cara; só carregar quando filtro = "todos". Para dia/semana sem resource, `timegrid-view` simples basta.

## Definition of Done

1. Visualização dia/semana/mês funcional.
2. Click em evento abre sheet. Sheet tem botões placeholder (Stories 2.5/2.6 ativam).
3. typecheck + lint + build verdes.

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-04-20 | @sm (Orion atuando) | Draft criado (não validado ainda). |
| 2026-04-30 | @po (Pax) | **Validação:** GO 7/10 com observações enriquecedoras (não bloqueantes). Adicionada seção **Dev Notes** com: timezone `America/Sao_Paulo`, locale `pt-br`, `requireRole('viewer')`, cap de 200 eventos com banner, política de mobile <375px, dica de performance no resource-timegrid. Status: **Draft → Ready**. |
| 2026-04-30 | @dev (Dex) | **Implementação:** 5 arquivos novos em `app/(app)/agenda/` + 2 componentes shadcn (`sheet`, `select`) + 5 deps FullCalendar. Toolbar custom (KEYRA design system), eventos via Server Action assíncrona, sheet de detalhes com placeholders para Stories 2.5/2.6, banner de truncate >200 eventos, locale pt-br + firstDay segunda. AC1, AC3, AC4, AC5 cumpridos. **AC2 parcial** — `resource-timegrid` é premium e foi substituído por filtro via Select (registrado em Dev Decisions). typecheck + lint + build verdes. Status: **InProgress → InReview**. |
