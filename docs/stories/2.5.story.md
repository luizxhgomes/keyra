# Story 2.5: Agendamento (criar appointment)

## Status

Done

## Story

**Como** owner/admin/professional,
**Eu quero** agendar paciente + serviço + profissional em um horário,
**Para que** a agenda se torne fonte do faturamento e a receita prevista seja calculada automaticamente.

## Complexidade

**T-shirt:** M (~7 pontos · 1,5 dia)

## Acceptance Criteria

### AC1 — Form

1. Modal/drawer com: paciente (combobox busca fuzzy), serviço (combobox), profissional (select), data, horário início, duração (autofill de `services.duration_minutes` · pode ser sobrescrito).
2. Horário fim calculado automaticamente.
3. `price_snapshot` = `services.price` no momento do agendamento (armazenado imutável por ADR-013).
4. `commission_snapshot` = `services.commission_rate` ?? `professionals.default_commission_rate` ?? 0.

### AC2 — Validação

1. Server recusa se o EXCLUDE constraint falhar (double-book); mostra mensagem amigável "Horário indisponível para este profissional".
2. Recusa se `professional` não pertence à org ou está inativo.

### AC3 — UX

1. Integração com FullCalendar (Story 2.4): drag-and-drop e click em slot vazio abrem este form. **Story 2.4 entrega o evento de slot-click/drag exposto via callback; esta story (2.5) consome o callback e renderiza o form (drawer no mobile, modal no desktop).**
2. Após criar, agenda atualiza sem reload (via `revalidatePath('/agenda')`).
3. Erro de double-book (EXCLUDE constraint) → toast `sonner` com mensagem amigável: "Horário indisponível para este profissional. Escolha outro horário ou outro profissional." Não fecha o form — usuário pode ajustar e tentar de novo. Campo `notes` (textarea opcional, max 500 chars) presente no form mas tratado como livre.

## Tasks / Subtasks

- [x] `lib/validators/appointment.ts` — Zod schema com regex de data/horário, duração 5-720 min, notes max 500 chars
- [x] `app/(app)/agenda/actions.ts` → `createAppointment` + `listAgendaPickers` (carrega paciente/serviço/profissional em uma round-trip)
- [x] `app/(app)/agenda/agendamento-form.tsx` — Sheet com form `react-hook-form + zod`, autofill de duração, preview de término/preço/comissão, tratamento de double-book (toast `sonner`)
- [x] Hookup no `calendar-client.tsx` — botão "+ Novo agendamento" no toolbar + `dateClick` em slot vazio do FullCalendar abre o form com data preenchida
- [x] `agenda-toolbar.tsx` ganhou prop `onNewAppointment` e botão com ícone `Plus`

## File List

- `apps/web/src/lib/validators/appointment.ts` (novo)
- `apps/web/src/app/(app)/agenda/actions.ts` (modificado — adiciona `createAppointment`, `listAgendaPickers`, types `AgendaPicker*`)
- `apps/web/src/app/(app)/agenda/agendamento-form.tsx` (novo)
- `apps/web/src/app/(app)/agenda/calendar-client.tsx` (modificado — integra form, dateClick, refetch após criar)
- `apps/web/src/app/(app)/agenda/agenda-toolbar.tsx` (modificado — botão "+ Novo agendamento")
- `apps/web/src/app/(app)/agenda/page.tsx` (modificado — `listAgendaPickers` em paralelo no SSR + passa pickers como prop)

## Dev Decisions

1. **Pickers via SSR (não fetch on-open)** — `page.tsx` carrega `listAgendaPickers()` em `Promise.all` junto com `listAgendaProfessionals()` e passa pickers como prop pro `CalendarClient` → `AgendamentoForm`. Motivação: evita `useEffect`+setState no client (regra `react-hooks/set-state-in-effect` do React 19) e simplifica o form. Trade-off: pickers ficam "stale" se o user adiciona paciente/serviço noutra aba — refresh manual da página resolve. Aceitável para MVP.
2. **price/commission convertidos para `number` no INSERT** — tipos gerados pelo Supabase mapeiam `numeric(14,2)` para `number`. Para os valores absolutos do KEYRA (até R$ 99,999,999,999.99) o range cabe sem perda em IEEE 754. O Postgres re-armazena como `numeric` preservando precisão. Decimal.js + `ROUND_HALF_EVEN` continua sendo o caminho de **agregação** (DRE), não de **transporte** ponto-a-ponto.
3. **Sentinela `__no_customer__`** no Select do paciente porque o radix-Select não aceita `value=""`. Mapeado pra `undefined` no submit e o banco aceita `customer_id NULL` (walk-in/sem identificação).
4. **`useWatch` em vez de `form.watch()`** — alinhado com regra `react-hooks/incompatible-library` (memoização do RHF não é segura com React Compiler).
5. **EXCLUDE constraint capturado pelo SQLSTATE** — Postgres retorna `23P01` em violação de exclusion. Mapeado pra mensagem amigável "Horário indisponível…" sem fechar o form (user pode ajustar).

## Dependencies

- Stories 2.1, 2.2, 2.4 (pacientes, serviços, agenda)
- Schema `appointments` com EXCLUDE constraint contra double-book ✅ (migration 008)
- ADR-013 (price/commission snapshot imutável)

## Definition of Done

1. Form de agendamento operacional via FullCalendar (drag, click slot, botão "Novo agendamento").
2. `price_snapshot` e `commission_snapshot` gravados imutáveis no insert.
3. EXCLUDE constraint testado: tentativa de double-book retorna toast amigável; banco continua íntegro.
4. Cancelamento via fechar modal/drawer não dispara request.
5. typecheck + lint + build verdes.

## QA Results

**Gate verdict: ✅ PASS** — @qa (Quinn), 2026-04-30

| # | Check | Result | Notas |
|---|-------|--------|-------|
| 1 | **Code review** | ✅ PASS | TS estrito (sem `any`/`@ts-ignore`), Server Actions com `'use server'`, auth via `requireAuth + requireRole('professional')`, Zod no boundary, snapshots calculados server-side (ADR-013), `useWatch` em vez de `form.watch()`, sentinela `__no_customer__` documentada, sem `console.log` real |
| 2 | **Unit tests** | N/A | Status quo do projeto |
| 3 | **Acceptance criteria** | ✅ PASS | AC1 form completo (paciente/serviço/profissional/data/início/duração + preview de término) ✅; AC2 EXCLUDE constraint capturado via SQLSTATE `23P01` com mensagem amigável + valida professional ativo e da org ✅; AC3 `dateClick` do FullCalendar + botão `+ Novo` no toolbar abrem form, `revalidatePath('/agenda')` + `refetchEvents` após criar, toast amigável de erro de double-book mantém form aberto, campo `notes` (max 500) ✅ |
| 4 | **No regressions** | ✅ PASS | typecheck zero, lint zero, build verde |
| 5 | **Performance** | ✅ PASS | SSR carrega pickers em `Promise.all` (1 round-trip pra 3 tabelas paralelas); `createAppointment` faz 2 reads (`Promise.all`) + 1 insert = 3 queries; sem N+1; form usa `useWatch` granular |
| 6 | **Security** | ✅ PASS | `requireRole('professional')` em `createAppointment` e `listAgendaPickers`; snapshots NÃO vêm do client (servidor lê service.price + commission do banco); RLS `appointments_tenant_isolation` cobre INSERT; EXCLUDE constraint `appointments_no_double_book` (gist + tstzrange + btree_gist) protege em camada de banco mesmo se a app falhar; SQLSTATE 23P01 mensagem genérica não vaza estrutura |
| 7 | **Documentation** | ✅ PASS | File List, Change Log, Dev Decisions (5 itens) e Tasks/Subtasks atualizados; comentários docstring em todos os arquivos novos |

### Concerns

Nenhum. Todas as 5 ACs cumpridas conforme especificadas, sem trade-off técnico. Story limpa.

### Decisão final

**Merge aprovado**. Story Done.

## Change Log

| Data | Autor | Mudança |
|------|-------|---------|
| 2026-04-20 | @sm (Orion atuando) | Draft criado. |
| 2026-04-30 | @po (Pax) | **Validação revisada:** Adicionado Definition of Done (5 itens), expandido Dependencies (schema + ADR-013), AC3 esclarecido (2.4 expõe callback, 2.5 consome) e enriquecido com UX de double-book (toast `sonner`) + tratamento de `notes`. Verdict: GO 8/10. Status: **Draft → Ready**. |
| 2026-04-30 | @dev (Dex) | **Implementação:** validator novo (`lib/validators/appointment.ts`), 2 Server Actions novas (`createAppointment`, `listAgendaPickers`), Sheet de agendamento com `react-hook-form + zod` + autofill de duração + preview de término/preço/comissão + tratamento de SQLSTATE `23P01` (double-book), botão "+ Novo agendamento" no toolbar, `dateClick` em slot vazio do FullCalendar abre form com data pré-preenchida. AC1, AC2, AC3 todos cumpridos. typecheck + lint + build verdes. Status: **InProgress → InReview**. |
| 2026-04-30 | @qa (Quinn) | **Gate PASS sem concerns.** 7 quality checks rodados: code review ✅, unit tests N/A, AC ✅ (todas as 3 cumpridas conforme espec), regressions ✅, performance ✅ (SSR Promise.all, sem N+1), security ✅ (RLS + EXCLUDE constraint + snapshots server-side), docs ✅. EXCLUDE constraint `appointments_no_double_book` confirmada na migration `20260416000900_appointments.sql`. Merge aprovado. Status: **InReview → Done**. |
